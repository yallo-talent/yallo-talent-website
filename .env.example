# Public site origin used for canonical URLs, OG tags and the sitemap.
# Set to the production domain in production only — any other value causes
# the site to serve noindex/nofollow metadata and a Disallow: / robots rule
# so preview and placeholder deployments are never indexed.
NEXT_PUBLIC_SITE_URL=https://yallo.co
