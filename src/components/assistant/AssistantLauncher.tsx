"use client";

// Placeholder so layout.tsx's deferred import resolves. Session B fills this
// in behind the same export; session A does not edit past this point.
export function AssistantLauncher() {
  return null;
}
