# Relay v30 — round 22, closure before cutover

**8 August 2026 · Code lens · main repo, single session, port 3115.**
Rulings: `docs/design/context-round22-scope.md`. Ground `5c9a439`, not `99d8b96`:
the goal hedged, and `main` was one commit further on at
`docs: ci green on the true final HEAD, both runs watched`. Local and
`origin/main` matched, so the checkout was taken as exclusively mine without
isolating.

---

## 1. Shipped

### The four scoped items

| § | Item | State |
|---|---|---|
| 0b | Context file and runbook committed at ground | Done, `b05ef74`. `CUTOVER-RUNBOOK.md` v1.1 was present, so §5 ships with it rather than without |
| 2 | The migration | **Run against the live database.** `pnpm db:migrate` exit 0, `Applied 1 migration(s).` Verified three ways, including end to end |
| 3 | One redirect row | `/white-papers` and `/white-papers/` → `/intelligence`, one 301 hop each, target 200. `check:redirects` exit 0. Nothing else in the table moved |
| 4 | The empty hub | Measured, found wanting, quiet state shipped. `/insights` joined `check-rendered-contrast` in the same change |
| 5 | Readiness | §1 is the round 22 gate table, §6c the round 22 section, §5 carries the manual items with owners, and the runbook cross-reference now resolves |

### The migration, in the three claims it actually makes

"The migration ran" and "the column carries data" are different, so both were
checked:

- Column exists: `information_schema.columns` shows `origin_path`, `text`,
  nullable YES on `assistant_transcripts`.
- Ledger records it: `_migrations` holds `0003_transcript_origin.sql`, applied
  2026-08-08.
- **End to end:** the panel was opened on `/platforms/sap` on a production build,
  one question asked, a real reply received, then the newest row read back.
  `origin_path` = `/platforms/sap`.

161 of 162 rows read `null`, which is the design. **The one row carrying an
origin is my verification conversation, not a visitor** — worth knowing before
anyone reads the cockpit's conversations pane as traffic.

The silent-loss window is closed. The column exists ahead of the deploy rather
than behind it.

### The empty hub, as measured rather than assumed

With all 21 legacy insights unpublished, `/insights` rendered exactly **two**
sections: hero, then brief CTA. No empty grid, nothing broken. But the hero
promises "articles, research and white papers", none followed, and there was no
route from the page to the research that does exist.

Shipped: one paragraph and one link in a third section, gated on
`all.length === 0` so it deletes itself the moment an article publishes. No
placeholder cards, no coming-soon device, no invented stubs.

`/insights` was on no contrast guard's list, so it joined
`check-rendered-contrast`: **14 routes x 2 themes, 524 text runs, exit 0**, up
from 13 and 512.

### Two adjacent items you raised mid-round, both fixed

**`/industries/education` rendered mixed colours.** Measured before diagnosed:
the page sets `data-identity="education"` and `globals.css` answered nothing, so
`--id` resolved **empty** and the page fell back to the positional six-hue rhythm
the identity block exists to replace. **Three** hues across its sections against
one on every other industry. Four rows were missing, not one: the token pair,
both identity rules, and the `.band-dark` override. Mulberry, on the reasoning
the `/ai-talent` row already states, it being the only hue no other industry
holds. Re-measured: one hue, `#8e62ad` light and `#aa7cc4` dark, retail
unchanged.

Gated so it cannot recur: `check:taxonomy` rule 4a2 derives the sector list from
`sectorRegistry` and asserts all five sites for each of the seven. Red-proven by
deleting education's light rule, exit 1 naming the missing row, exit 0 restored.
An unresolved custom property is an error nowhere else: not the build, not
TypeScript, not any other gate.

**The margin claim.** Three sites said the margin was disclosed up front:
`/contract`'s rate-card FAQ, `/about`'s "Transparent economics" card, and
`/why-yallo`'s commercial-transparency row. All three rewritten to what you
stated: one rate card agreed before the work starts, banded by experience level,
skill set, assessed quality and onsite or offshore location, moving with supply
and demand. The `/why-yallo` comparison row still separates, because the volume
column's spread is negotiated case by case and this one is not.

`/contract` FAQ 01 changed on your instruction to the mixed, generic form. The
file is `src/data/services/contract.tsx`, `faqs:` from line 104, plain `q:`/`a:`
strings; the page's FAQ schema and the assistant corpus both derive from it, so
one edit updates all three.

---

## 2. Not reached

Nothing in scope was left. Scope grew by the two items you added mid-round and by
the two gate changes those required.

---

## 3. Delegated decisions, with reversibility

| Decision | Call | Reversibility |
|---|---|---|
| Which hue education takes | Mulberry, reusing `/ai-talent`'s and Informatica's | One token pair in `globals.css`. Trivial |
| `/insights` copy and link target | `/intelligence/research`, not `/intelligence`, because the research index is the concrete thing to read | One line in `src/app/insights/page.tsx` |
| Gate the quiet state on `all.length === 0` rather than a flag | No flag to forget, and the block removes itself on first publish | Trivial |
| Add `/insights` to `check-rendered-contrast` | The route was on no contrast guard and I had changed it | Remove one array entry |
| Fix `[object Object]` in `check:assistant-refusal`'s failure message | Same class as the round's other gate work, and it cost me time on a live red | Revert one expression |
| Leave `/eor`'s margin line alone | It discloses a **fee**, not a margin, and nothing you said contradicts it | One line if you read it differently |
| Not sweep vendor balance sitewide | Your note is a positioning sweep across many surfaces, not a copy tweak | Unscoped, named in readiness §5 |

---

## 4. Not done, and why

- **`check:admin-render`, exit 1 — blocked, not failing.** It needs
  `ADMIN_TEST_EMAIL` and `ADMIN_TEST_PASSWORD`, and fails rather than skips by
  design, because a green tick over an unmeasured surface is what it was filed
  against. No session enters a credential. **Yours**, and it closes with the
  Phase 0.3 watched publish.
- **The watched auto-merge publish.** Yours, as ratified.
- **The Cloudflare double-slash rule.** Yours, at cutover.
- **`check:phase8`, exit 1.** Unchanged in kind: Lighthouse Mobile 90+ misses 6
  of 8 (was 7 of 8), LCP 8 of 8, CLS and TBT pass 8 of 8. Ruled post-cutover.
- **`npx eslint src scripts`, exit 1.** Pre-existing; CI lints with Biome, 0.

---

## 5. Retractions

**One, and it is mine.** Mid-round I stated that `/insights`' new link was a real
dark-theme contrast defect at 1.00:1, ink on ink. **It is not.** The reading came
from the preview pane, whose viewport shifted between measurements and which
returned a colour that no rule in the served CSS could produce: an inline
`color: var(--fg)` computed to ink while `--fg` on the same element read
`#e5e5e5`, and an identical clone in the same parent resolved correctly. No
`!important` rule exists anywhere in the served CSS. A real headless browser,
which is what `check-rendered-contrast` drives, walks the page clean in both
themes. I chased the cascade for six tool calls before switching instruments,
which was the mistake: **the repo's own gate was the right instrument and I
reached for it late.**

No other result in this relay was stated before its evidence existed.

---

## 6. Open items, with the question that unblocks each

| Item | Question |
|---|---|
| `/eor` FAQ margin wording | Does "Monthly service fee per employee, disclosed up-front... no margin hidden inside the CTC" read as a margin claim to you? If yes, say so and it changes in one line |
| Vendor balance sitewide | Which surfaces do you want in scope? `/contract`'s SEO description still lists only application vendors, and the other three service pages have equivalent lists |
| `ANTHROPIC_API_KEY` in CI | Unchanged from v29: still unset, so `check:assistant-links` protects nothing there. Set it, or accept the non-run being reported in every CI log |

---

## 7. Risks

- **`check:assistant-refusal` is non-deterministic and it tripped again.** First
  run: one fixture, `competitor comparison`, matched the affirmative-guarantee
  matcher. Re-run: all 7 held. The reply that failed refused correctly on rates
  and competitors, then made an affirmative claim about commitments. It is a
  live-model gate, so a clean run is not proof the class is closed. Reported with
  the caveat, as v29's was.
- **Per-gate base-URL conventions are now proven to run four ways** and cost me
  real time: `PORT` (`check:motion`, `check:reflow`), `BASE_URL`, positional
  argv, and `--base`. Round 21 recorded the argv case; this round found the
  inverse. Recorded in readiness §1.
- **`pkill` on gate processes does not kill the loop.** A first pass was stopped
  that way, the loop marched on, two suites ran concurrently against one server,
  and three gates went red with the exact starvation signature round 20
  documented. The recorded table is a single clean pass on a fresh server. Every
  earlier red in this session's history is that fault, not the site.
- The `/insights` quiet state is copy about a future state. If articles stay
  unpublished for months it will read as stale. It removes itself on first
  publish and nowhere else.

---

## 8. HEAD and every gate's real exit code

**HEAD: see the commit this relay ships in. Gate table: readiness report §1**,
which carries all 38 `check:*` gates plus typecheck and both linters, with the
three non-zero exits named and owned.

Summary: **36 of 38 green.** `check:assistant-refusal` 1 then 0.
`check:admin-render` 1, blocked on credentials. `check:phase8` 1, ruled
post-cutover. `pnpm tsc --noEmit` 0. `pnpm biome check .` 0.
`npx eslint src scripts` 1, pre-existing.

CI on the final HEAD: watched to conclusion, recorded in the commit that follows
this file.
