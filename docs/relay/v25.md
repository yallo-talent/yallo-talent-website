# Relay v25 — Code to Chat, round 17

**6 August 2026 · single session · main, no worktree · port 3115 · one dist directory (`.next`)**
Contract: `context-round13-scope.md` §8.5 as amended by round 17 §1.1. Every section
below is present, including the empty ones.

`main` at `1e30a75`. Ground was **`c314d0f`, not `90a1e95`** — the relay v24 commit
was already on `main` and pushed. One branch checked out, one worktree, nothing
untracked I would have lost. `docs/gtm/platform-employer-signals-2026-08-02.md`
stays untracked, per §2.4.

**Port 3115 was occupied at the start by a `next-server` from a previous session,
one hour old, serving a stale build on the non-production robots branch.** Killed
before any measurement. This is the third round the standing rule has caught it.

---

## Shipped

**Round 17 rulings and R-A9 committed first** (`c3d5c5d`), before any work
depended on them, with R-A9 recorded in `AGENTS.md` including its carve-out.

**§2.1, the robots production branch — measured for the first time** (`483e161`).
`NEXT_PUBLIC_SITE_URL` is inlined at build, so every build anywhere had exercised
only the blanket-disallow branch. Built with the production origin and asserted
the emitted file: 15 named user-agents plus `*`, each with `Allow: /` and all four
disallowed paths, one sitemap line.

`check:robots` parses rather than greps — `Disallow: /` and `Disallow: /api/` both
contain the substring `Disallow: /`, and telling a blanket lockdown from a path
exclusion is the one distinction the gate exists to make. The policy moved to
`src/lib/robots-policy.json`, read by `robots.ts` and by the gate; the production
origin moved with it, out of `seo.ts`.

Proven red three ways, real exit 1 each: production artefact read with the env
unset, `/downloads/` dropped from the emitted file, a production build emitting
the lockdown. Green on both branches. CI asserts non-production on the build it
already does; `robots-policy.yml` asserts production behind a path filter on the
four files that can move it, plus `workflow_dispatch` for cutover and a weekly
cron.

**§2.2, one dated attribution line** (`38eda93`). Rendered beneath every metrics
block on five templates:

> Yallo internal delivery, shortlist and placement records and programme
> register, as at 30 July 2026.

Composed from the same `source` and `asAt` fields it names, never typed.
`src/data/metrics.ts` throws at build time if the four stop reducing to one line.
`asAt` unmoved. `refreshOwner` entered the schema as **"Head of Talent
Acquisition"**, a role.

**The gate found a real defect the moment it ran, on seven pages, and it is the
eleventh instance in a new place.** `/platforms/*` published "2:1", "80%" and
"72h" as first-party claims with no source and no date, from figures **typed into
`src/data/platforms/why.ts`** where the quarterly refresh could never reach them.
The comment above that rail said three figures were derived from platform
coverage and one was a canon metric; the reverse was true, which is how it
survived. Fixed under §8.3 as the same class: `publishedFigure()` reads them from
`content/metrics.yaml` and throws on an unknown label. `check:metrics-attribution`
now green across 105 pages, and it holds no copy of the expected line — owner,
record kinds and date all come out of the YAML.

**§2.1, the PDF can no longer disagree with its own pages** (`26efd4f`). Not a
byte diff: two runs of the same content produced two different SHA-256s, so byte
comparison fails every time and teaches everyone to skip the gate. A manifest with
two links instead, both written by the one run that printed the PDF — size and
SHA-256 bind the file, a whitespace-collapsed text fingerprint binds the content.

The extractor strips `<script>` before hashing, and that is not tidiness:
`body.textContent` includes the RSC hydration payload with the build id and chunk
hashes in it. The first version fingerprinted 78,136 characters instead of 16,216
and would have failed on every rebuild. Proven red three ways, real exit 1 each.
**Then it earned itself the same day**: Sumeet's attribution copy change moved the
print surface and the gate named the drift before anyone thought about the
document.

**§2.3, the Admin cockpit — read panes complete** (`9c033da`). `/admin`, Auth.js
v5, one identity, three panes, route group `(cockpit)` so the layout's `auth()`
guard runs before any child renders or queries, and a pane added tomorrow inherits
it by being in the directory. `/admin/sign-in` sits outside the group because a
sign-in page inside its own guard redirects to itself.

No plaintext password anywhere: `pnpm admin:hash` reads it with terminal echo off
and prints only a scrypt hash. The verifier runs the email and password
comparisons unconditionally, because returning early on a wrong email makes the
response measurably faster for an unknown address than for the real one. Auth
config is checked at call time, not import time, so `next build` still works in
environments with no admin secrets.

`check:admin-isolation` **proven red six ways**, real exit 1 each: `/admin/`
dropped from the robots policy, a corpus source file naming `/admin`, the hash
script's scrypt parameters drifting from the verifier's, a sitemap entry, a
homepage link, and — with a rebuild for the last two — the layout guard removed
so panes answered 200 with content anonymously, and the middleware admin branch
removed so the noindex header vanished from all five routes.

**The corpus assertion is the one §2.3 said would be forgotten, so it has the most
reasoning behind it.** A `.mjs` gate cannot import the TypeScript corpus builder,
so it sweeps the trees the corpus is generated from — `src/data/**`, `content/**`,
`src/lib/assistant/**` — for the admin base path, deliberately over-approximating.
Word-boundaried, which mattered immediately: `/privacy` now says a named
administrator can read conversations, and "administrator" must not trip a gate
looking for `/admin`.

**§2.4, the tail** (`48e2acc`). `api/cv` sends from `brief@yallo.co`. Radwell's
assetless row retired from `content/clients.yaml`, and two comments that named it
went with it. The two empty `RESEND_*` lines and local `RESEND_TO` untouched.

**`check:crawlers` retries once and reports it** (`1e30a75`). See risks.

**Sumeet's four mid-round instructions, all executed and verified in served HTML.**
The coverage-note paragraph and the Method section off the research pages; the
closing links arrowed; "measured as at 2 August 2026" reduced to "August 2026";
"Source: LinkedIn Talent Insights"; padding above the attribution line.
`LTI_AS_AT_MONTH` is generated from the same `AS_AT` the full date is, so neither
is typed.

---

## Not reached

**The case-study write pane.** Create and reorder are unstarted. This is §2.3's
stated stop condition, taken deliberately: the read panes are complete and
verified, and a half-built write path committing to `main` is worse than none. It
was also unverifiable — no GitHub token had arrived by the time the read panes
were done, and I will not report a commit path as working without having watched
it commit.

**`check:assistant-*`.** Four of the five need a running assistant and an API
key; `check:assistant-terms` crashed on `ECONNREFUSED` against its default port
3000 before I noticed the base-URL convention. Not rerun. Nothing this round
touched the assistant except `corpus.ts`'s one date string.

**Phase 8.** Descheduled, R-A6. Not touched, not measured.

---

## Decisions taken under delegated authority

| Decision | Reversibility |
|---|---|
| **Reordering will rewrite `content/case-studies/order.yaml`, not an `order` frontmatter field.** §2.3 asked for an `order` field because "order currently falls out of the directory and dates". That premise is stale: `order.yaml` already exists, `src/lib/case-study-order.ts` already enforces it, and its own doc comment records that a per-file `featured` integer was REPLACED by this file because reranking meant editing fourteen files. Adding the field back would restore the defect a previous round removed. | Free. No code written either way yet. |
| The attribution line is sentence case, not the site's uppercase mono source treatment. That treatment is for one-word labels; this is a full sentence and sets to two shouting lines at 0.12em. | One CSS block, three files. |
| The attribution is a **required** prop on every template that renders the block, so a missed call site is a type error. `tsc` named two I had missed. | Change to optional, one line. |
| Day precision survives where it is a statement about when the extract was read — the chart captions and the print footer sentence — and only the attribution labels went to month precision. Sumeet named "measured as at 2 August 2026", which is the label. | One constant swap per site. |
| Three noindex mechanisms for `/admin` rather than one, and the middleware branch overrides the production/non-production switch. | One conditional. |
| `check:crawlers` retries a refused gated probe once and prints that it did. Two refusals in a row still fail. | One block. |
| The PDF manifest lives at `content/research-pdf.manifest.json`, outside `public/`: a gated asset's provenance is not itself a published file. | Move and one constant. |

---

## What I did not do, and why

**I did not remove Wickes from `content/clients.yaml`.** It is the identical case
to Radwell — listed, consented, no asset, silently dropped by `hasLogoAsset()`.
§8.3 would call that the same class and put it in scope. **R-A9 outranks §8.3
here**: whether a client's name appears on the site is a publishing decision, and
extending a ruling about one named client to another by analogy is exactly the
editorialising R-A9 forbids. One line in `content/clients.yaml` when you say so.

**I did not derive the typed metric figures in `src/data/home/hero.ts` and
`src/data/home/engage.ts`.** Same class as the `why.ts` defect above — `"2:1"`,
`"72h"` and `"50+"` typed into home data, beyond the quarterly refresh — and I
fixed every instance I could reach. These two are blocked by a real boundary:
`Instrument.tsx` and `Engage.tsx` are client components, and `src/data/metrics.ts`
reads the file system, so it cannot cross into them. The fix is a generator —
`content/metrics.yaml` to a client-safe generated module with a `--check` flag,
exactly the shape `build-research-dataset.mjs` already proved. That is a unit of
its own and the run was carrying the cockpit. **P1, and the last known instance of
this class on the site's own figures.**

**I did not add a browser gate for the `/admin` panes' accessibility or type
scale.** They are absent from the sitemap, so every enumerating gate reads its
route list from `publishedPaths()` and none visits them; `check:gate-coverage`
lists all five as units with no live URL and passes. The CSS uses only existing
tokens and the contrast gate is source-level, so it covers them. But **no rendered
a11y or type-scale measurement has been taken on any admin pane.** Filed, severity
P2: it is an internal surface with one user, and the same argument was made about
`/ai-talent` before it shipped six classes under the type floor.

**I did not verify a successful sign-in.** No credentials arrived. Everything an
anonymous caller sees is verified — all four guarded routes 307 to sign-in with
`noindex` headers, sign-in renders 200, no pane content served — but the
authenticated path has never executed. It will need one sign-in before cutover,
and the sign-in page reports by name which variables are missing.

**I did not touch `.env.local`, and never printed or echoed a value from it.** I
listed its KEY NAMES once to confirm `NEXT_PUBLIC_SITE_URL` was set before
overriding it on the build command.

---

## Retractions

**None.** Nothing in v24 has turned out to be wrong, and nothing I reported
earlier in this round needed correcting. One thing came close and is worth
recording as a near miss rather than a retraction: the first PDF fingerprint
included the hydration payload and would have made the gate fail on every
rebuild. It was caught by reading the character count — 78,136 against an
innerText length of 16,391 — before the gate was committed, not after.

Two of my own perturbations failed to perturb and I noticed both: a `perl`
substitution against the sitemap missed because the file is newline-separated
between tags, and the first red proof of `check:metrics-attribution` reported
`EXIT=0` because `$?` after a pipe is `head`'s status. Both re-run properly. Every
exit code in this relay is from a command whose status was captured directly.

---

## Anything I needed from another session's territory

**Nothing.** Single session, one branch, one worktree, no per-session dist
directory created and therefore none to clean up — plain `.next` throughout, per
`AGENTS.md`.

---

## Open items, each with its unblocking question

1. **The write pane.** Unblocking: a fine-grained GitHub token with Contents read
   and write on this repository only, in `.env.local` as `ADMIN_GITHUB_TOKEN`, and
   the admin login as `ADMIN_EMAIL` plus `ADMIN_PASSWORD_HASH` from `pnpm
   admin:hash`. Then: does branch protection on `main` accept it, or does the
   cockpit need a branch and a PR?
2. **GitHub's fine-grained tokens do not offer path scoping.** §2.3 asked for
   `content/`-only "if the platform permits". It does not. Unblocking question:
   enforce the `content/` restriction in the cockpit's own commit path — which a
   compromised cockpit could bypass, since it holds the token — or accept a
   repo-wide contents-write token, or use a GitHub App with a narrower
   installation? **My recommendation: enforce in the commit path AND require the
   cockpit to open a PR rather than push to `main`**, so the path restriction has
   review behind it rather than only code.
3. **Wickes.** Retire the row, or supply the asset? One line either way.
4. **`/privacy`'s new sentence**, logged for your veto as §2.3 requires: "Recorded
   conversations can be read by one named administrator at Yallo, and by nobody
   else."
5. **The generated-metrics module** for `hero.ts` and `engage.ts`. Schedule it, or
   accept three typed figures on the homepage until after cutover?
6. **`check:assistant-*`** never ran this round. Worth a pass before cutover.
7. Still outstanding from v24 and untouched: the heading fade on `/permanent` and
   `/contract`, and **R-AI3**.

---

## Risks

**`check:crawlers` probes a live third-party CDN, and it is a cutover gate.** One
run of three refused `ChatGPT-User` alone. The retry now absorbs a single
transient and prints when it did — but if that line starts appearing every run,
the zone is intermittently refusing an Agent crawler and that is a real finding,
not noise. Worth reading, not skipping.

**The client-bundle leak assertion in `check:admin-isolation` did not execute
here**, because no admin secrets are set in this environment. It says so out loud
rather than passing silently. It must be run once with the real environment before
cutover, and that is the only assertion in that gate with no observed result.

**`next-auth` is on `5.0.0-beta.32`.** Auth.js v5 is the golden path §2.3 named and
the beta is what v5 currently is, but it is a beta in a dependency on the auth
path. Pinned exactly, not caret-ranged.

**The admin panes are unmeasured for rendered accessibility and type scale.** See
above.

**`robots-policy.yml` has never run.** It is a new workflow with a path filter, and
the commit that introduces it changes `src/app/robots.ts`, `src/lib/seo.ts` and
`src/lib/robots-policy.json` — so the filter should fire on push to `main`. If it
does not, the filter is wrong and the production branch is again asserted nowhere
but this session. First thing to check after this push.

---

## HEAD and every gate's real exit code

**HEAD `1e30a75`.** All against a production build (`NEXT_PUBLIC_SITE_URL=https://yallo.co`)
served by `next start` on port 3115, run serially.

| Gate | Exit | Note |
|---|---|---|
| `check:crawlers` | 0 | 15 crawlers served by yallo.co; 1 probe needed its retry |
| `check:no-redirects` | 0 | 238 hrefs across 174 pages |
| `check:motion` | 0 | |
| `check:marks` | 0 | worst deviation 34.4% |
| `check:estate` | 0 | |
| `playwright` (3 specs) | 0 | 12 passed |
| `check:robots` | 0 | **new**; production branch, 15 groups + `*`; red-proven 3 ways |
| `check:metrics-attribution` | 0 | **new**; 105 pages; red-proven on 7 real defects |
| `check:research-pdf` | 0 | **new**; red-proven 3 ways, then caught a real drift |
| `check:admin-isolation` | 0 | **new**; red-proven 6 ways; client-bundle assertion not exercised |
| `check:terms` | 0 | |
| `check:contrast` | 0 | |
| `check:type` | 0 | |
| `check:type-render` | 0 | 18 templates x 4 widths, 480 pairs |
| `check:a11y` | 0 | 24 routes x 2 themes x 2 widths, 1 exemption with its clause |
| `check:reflow` | 0 | 174 routes x 2 themes at 320 and 360 |
| `check:interaction` | 0 | 7 templates x 2 widths, 630 focused stops |
| `check:visual` | 0 | |
| `check:yallo-case` | 0 | 174 pages, 238 links resolve |
| `check:taxonomy` | 0 | 261 files |
| `check:prose` | 0 | |
| `check:cs-excerpts` | 0 | self-test then corpus |
| `check:research-dataset` | 0 | |
| `check:asset-case` | 0 | |
| `check:gate-coverage` | 0 | 10 lists read from source, none stale |
| `check:nav-promise` | 0 | |
| `check:cta-collision` | 0 | 4 routes at 360 and 1280 |
| `biome check .` | 0 | 6 warnings, all pre-existing |
| `tsc --noEmit` | 0 | |
| `pnpm build` | 0 | |
| `check:phase8` | not run | descheduled, R-A6 |
| `check:assistant-*` | not run | see "not reached" |

`eslint` not run: CI lints with biome and §8.2 says eslint gates nothing here.
