# Relay v21-A — Round 13 session A, foundations and integrator

**Branch** `feat/round13-foundations` · **Port** 3113 · **NEXT_DIST_DIR** `.next-a-r13`
**HEAD** `48e6cc5717d477eeaeccdcdaa3863b5fec59ddb8` (pushed to `origin/feat/round13-foundations`)

---

## Shipped

- **§0 grounding.** `origin/main` = local `main` = `158d8ad`, no discrepancy this round. Committed the three round 13/14 context docs by explicit path. The five `scripts/audit-*.tmp.mjs` scratch files round 13 scope expected did not exist in the tree — reported as absent rather than deleted. Cut both branches, spawned B's worktree at `../yallo-talent-website-B`. `ROUND13-SPLIT-OK`.
- **First commit landed the two seams** before B needed them: `layout.tsx`'s deferred launcher mount (via a client wrapper, `AssistantLauncherMount.tsx` — `ssr:false` is not allowed directly in a Server Component, discovered when `pnpm build` failed on the first attempt and fixed in a follow-up commit), and the capture contract (`briefFormSchema` extended with `source`/`transcriptId` — see Retractions below on the field name).
- **§4.1 durable capture layer.** L1 closed: `api/brief` and `api/cv` now persist to a new append-only Neon Postgres `submissions` table (`@neondatabase/serverless`) before attempting delivery; `ok: true` is only reachable once the row exists, so a missing `RESEND_API_KEY` or a Resend failure degrades to `{ok: true, delivered: false}` on a captured row, never a lost one. Referrer from the request header, UTM params read client-side at submit time (no query string on the API request itself). No CRM integration, per the ruling.
- **§4.2 nav card + gate.** Intelligence's featured card repointed at `/intelligence`, compensation language removed. New `check:nav-promise` gate, scoped to `nav-config.ts`'s hand-written copy (not wired into `check:terms`'s sitewide sweep — tried that first, got 19 false positives against already-correct copy like "day rate on your invoice"). Self-tested against both the original defect and a reintroduced one.
- **§4.3 axe findings, fixed by class.** `p-as-heading`: both `<p>` instances converted to `<span>`, matching `TheGap.tsx`'s existing (never-flagged) pattern for the identical stat-callout shape. `focus-order-semantics`: found two more instances beyond the one measured (`L1PageShell`'s own insights rail, `L2PageShell`'s) by grepping every `tabIndex={0}` scrollable container; all three converted to `<section>` with `aria-label`, matching `CaseRail`'s already-working pattern. Verified clean across 4 routes × 2 themes × 2 widths.
- **§4.4 gate class fix.** `check-yallo-case` and `check-reflow` now derive their route list from the live `/sitemap.xml` (shared `scripts/lib/published-paths.mjs`) instead of a hand list. The exhaustive run immediately found three more instances beyond the known one: `/managed-delivery`'s trust line and two more spots on `/why-yallo`. All four fixed. `check-gate-coverage.mjs` updated so it doesn't misreport these two gates as having lost their list. Audited every other enumerating gate; disposition and reasons in the §4.4 commit and below.
- **§4.5 italic font split + measurement.** Newsreader split into a normal-only face and a new weight-600-italic-only face, still preloaded. Measured `check:phase8 --passes 4`, worst-pass, Lighthouse 12.8.2, on a settled build, before and after. Tables below.
- **Merge.** `feat/round13-assistant` merged into `feat/round13-foundations`. Four real conflicts, all pre-adjudicated, resolved per §3.2. Read every file the merge touched, not just the conflicts — found and fixed one real cross-branch seam mismatch (below).

---

## What I did not do, and why

- **Did not chase the LCP miss further**, per §4.5's own instruction. The remaining causes (render-blocking CSS, font delivery generally) are their own round; `cssChunking: 'strict'` was already confirmed unable to fix the CSS half on Turbopack in round 12.
- **Did not convert `check-rendered-type.mjs` or `check-interaction.mjs` to exhaustive route sets**, despite auditing them under the same §4.4 reasoning. `check-rendered-type` is genuinely scoped by its own stated principle (one page per template/shell, since the defect is shared-stylesheet-level and same-shell routes fail identically — `check-gate-coverage.mjs` already audits shell coverage for it). `check-interaction` is scoped by measured cost: its existing 6-route list already took over 120s (real interaction simulation, not a DOM read), so the full 167 would cost tens of minutes for tests where two of its four assertions probe shared chrome that doesn't vary by route.
- **Did not convert `check-a11y.mjs`.** Its own docstring claims "every surface" while sampling 6 routes — the same shape of gap `check-yallo-case` had — but measured cost (~2.6s/route × 4 combos ≈ 29 minutes exhaustive) and the fact it isn't in `check-gate-coverage.mjs`'s tracked set at all make this a bigger structural change than this round's mandate. Flagging as an open item rather than deciding it unilaterally.
- **Did not rotate the Anthropic API key** B's relay flagged as briefly exposed in B's own tool output. Not something I can do; surfaced directly to Sumeet, who chose to handle it on his own timeline.

---

## Retractions

1. **The seam contract's field name was wrong.** My first commit extended `briefFormSchema` with `transcriptRef`. B, working before that commit was visible, built `assistantBriefPayloadSchema` against `transcriptId` — a reasonable independent guess, and B's own relay logged the mismatch as an open item for me rather than silently accepting it. Found by reading B's `capture.ts` during merge verification, not by a gate: had this shipped as-is, every assistant-sourced brief would have silently lost its transcript reference on arrival at the capture layer (zod strips unrecognised keys). Fixed by renaming the schema field to `transcriptId` — B's client and gate scripts were already built and tested against that name, so aligning my side was less rework than asking B to change five already-verified files.
2. **The Phase 8 "before" run was discarded and re-measured, twice.** Both times, session B's own `next build`/dev-server processes were confirmed (via `ps aux`) actively competing for CPU on the same machine during the run — the identical failure mode round 12's own precedent names ("the discarded contaminated run"). Neither partial run's numbers are reported anywhere below; both were thrown away in full and re-measured from a confirmed-idle baseline.

---

## Decisions taken under delegated authority, each with its reversibility

1. **Dropped session B's `.claude/launch.json` `round13-b` entry as a duplicate** of my own `session-b-r13` (identical port, identical dist dir, different name only). **Reversible, trivial** — either name works identically; picked mine to match the established `session-a/b/c` convention.
2. **Removed `src/components/assistant/AssistantMount.tsx`** (B's own temporary `ssr:false` bridge, explicitly described in B's relay as provisional, reverted out of every one of B's commits, confirmed unreferenced anywhere in the merged tree). **Reversible** — it's recoverable from git history if a reason ever appears to want it back, but nothing calls it.
3. **Added `ANTHROPIC_API_KEY` to `.env.example`** and **registered `check-assistant-a11y`/`check-assistant-bundle` in `check-gate-coverage.mjs`'s `GATES` array**, both per B's own logged open items and both squarely inside my owned files (`.env.example`, `scripts/**`). **Reversible, additive-only.**
4. **Added four `ALLOWED_LINES` exemptions to `check-terminology.mjs`** so it stops flagging `system-prompt.ts`'s own forbidden-vocabulary instruction (the model has to see the literal banned word to know what to refuse). **Reversible** — narrow, four exact-substring matches, verified they exempt only the four specific lines that tripped and nothing else.

---

## Anything I needed from B's territory

Nothing outstanding — B's relay's four open items for me (durable transcript store, the mount/schema seam, `check-terminology.mjs` extraction, `check-gate-coverage.mjs` registration) are addressed above except the transcript store, which correctly stays open: it needs a design decision (same table family as `submissions`, or separate?) that's Sumeet's to make, not mine to guess at mid-merge.

---

## Open items for Sumeet

1. **Transcript persistence (12-month retention, §6)** — nothing in either session's territory owns this yet. **Blocks the pilot gate regardless of anything else in this round.** Needs a decision: same table family as `submissions`, or its own.
2. **`check-a11y.mjs`'s route-sample gap** — see "What I did not do" above. Its own docstring claims exhaustive coverage; it samples 6 routes. Converting it is a bigger, costlier change (~29 min exhaustive) than this round's mandate covers — flagging for a decision, not deciding it here.
3. **The Anthropic API key exposure** B's relay logged — rotation is Sumeet's to do; he's chosen to handle it on his own timeline.
4. **B's three open items carried from `v21-B.md`**: the `--lift` reserved-shadow confirmation, launcher/StickyBriefCTA simultaneous-visibility check, and the keyboard-reachability distance (77–84 tab stops) UX call. None are mine to rule on.
5. **Worktree/branch cleanup** — `../yallo-talent-website-B` and `feat/round13-assistant` still exist. Following round 12's own precedent ("worktree removed and branches deleted only after ancestry confirmation"), I've confirmed `feat/round13-foundations` contains every commit from `feat/round13-assistant` (the merge), but I'm holding off on deleting the worktree/branches until you confirm — that's a step I'd rather have explicit sign-off on than assume.

---

## Risks

- **Non-determinism in the assistant's live-model gates** (refusal/grounding/terms) — inherent to testing generative output, not a one-time proof. B's relay already flags this; carrying it forward rather than re-discovering it.
- **The font split's LCP gap is real and unclosed.** Every route's worst-pass LCP still exceeds 2.5s after the split. The remaining levers are render-blocking CSS and font delivery generally, both explicitly deferred to their own round by §4.5.
- **Machine-level measurement contention.** Two sessions sharing one physical machine, even in separate worktrees, can invalidate a CPU-throttled Lighthouse measurement — not just a git-checkout exclusivity problem, which is the framing round 12's precedent used. Worth stating explicitly for future rounds: check `ps aux` for the *other* worktree's build processes, not only for `next start` on the shared checkout.

---

## Phase 8 — before / after (font split), worst pass, Lighthouse 12.8.2

### Before (font split not yet applied)

| Route | Perf p1 | Perf p2 | Perf p3 | Perf p4 | LCP p1 | LCP p2 | LCP p3 | LCP p4 | CLS | TBT | A11y | BP | SEO |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/` | 99 | 85 | 85 | 86 | 2.13s | 4.38s | 4.38s | 4.22s | 0.000 | 10ms | 97 | 100 | 69 |
| `/contract` | 86 | 87 | 87 | 93 | 4.08s | 4.08s | 4.08s | 3.18s | 0.016 | 13ms | 100 | 100 | 66 |
| `/platforms/sap` | 85 | 87 | 85 | 86 | 4.37s | 3.94s | 4.37s | 4.12s | 0.001 | 24ms | 100 | 100 | 66 |
| `/capabilities/data-analytics` | 86 | 86 | 86 | 86 | 4.21s | 4.21s | 4.21s | 4.22s | 0.000 | 11ms | 100 | 100 | 66 |
| `/industries/retail` | 86 | 87 | 89 | 86 | 4.21s | 4.09s | 3.78s | 4.21s | 0.000 | 11ms | 100 | 100 | 66 |
| `/industries/retail/customer-experience` | 86 | 93 | 86 | 93 | 4.23s | 3.17s | 4.21s | 3.17s | 0.000 | 11ms | 100 | 100 | 66 |
| `/case-studies/...hfm...` | 90 | 94 | 90 | 93 | 3.63s | 3.02s | 3.62s | 3.17s | 0.040 | 10ms | 100 | 100 | 69 |
| `/brief` | 87 | 87 | 94 | 93 | 4.06s | 4.06s | 3.02s | 3.17s | 0.003 | 8ms | 100 | 100 | 66 |

**Gate:** Lighthouse Mobile 90+ MISS 7 of 8. LCP < 2.5s MISS 8 of 8. CLS PASS 8/8. TBT PASS 8/8.

### After (font split applied)

| Route | Perf p1 | Perf p2 | Perf p3 | Perf p4 | LCP p1 | LCP p2 | LCP p3 | LCP p4 | CLS | TBT | A11y | BP | SEO |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `/` | 99 | 88 | 86 | 86 | 2.12s | 3.79s | 4.08s | 4.08s | 0.000 | 10ms | 97 | 100 | 69 |
| `/contract` | 88 | 88 | 88 | 87 | 3.92s | 3.92s | 3.93s | 3.93s | 0.016 | 11ms | 100 | 100 | 66 |
| `/platforms/sap` | 88 | 87 | 87 | 88 | 3.93s | 4.07s | 4.07s | 3.92s | 0.001 | 13ms | 100 | 100 | 66 |
| `/capabilities/data-analytics` | 88 | 88 | 88 | 88 | 3.93s | 3.92s | 3.92s | 3.92s | 0.000 | 14ms | 100 | 100 | 66 |
| `/industries/retail` | 88 | 87 | 88 | 87 | 3.92s | 3.93s | 3.93s | 3.93s | 0.000 | 8ms | 100 | 100 | 66 |
| `/industries/retail/customer-experience` | 90 | 88 | 90 | 90 | 3.62s | 3.92s | 3.62s | 3.63s | 0.000 | 15ms | 100 | 100 | 66 |
| `/case-studies/...hfm...` | 93 | 98 | 92 | 95 | 3.17s | 2.41s | 3.32s | 2.86s | 0.040 | 9ms | 100 | 100 | 69 |
| `/brief` | 94 | 92 | 94 | 98 | 3.02s | 3.32s | 3.02s | 2.41s | 0.003 | 10ms | 100 | 100 | 66 |

**Gate:** Lighthouse Mobile 90+ MISS 6 of 8 (`/brief` newly clears 90). LCP < 2.5s MISS 8 of 8 — every route's worst-pass LCP fell roughly 0.3–0.5s, not enough to cross the 2.5s ceiling anywhere. CLS and TBT unchanged, both passing.

**A partial improvement, still a miss on LCP — the outcome §4.5 itself predicted for a lever worth a fraction of 63 KiB on two of eight routes.** Not chased further, per that section's instruction.

---

## Gates run, and the real exit code of every one

| Gate | Exit | Notes |
|---|---|---|
| `pnpm typecheck` | 0 | Run after every commit and again post-merge. |
| `pnpm build` | 0 | Full production build, merged tree, 409 routes. |
| `check:terms` | 0 | Tree-wide, post-merge: 263 files, clean after the `system-prompt.ts` exemption fix (was 6 findings, all false positives from the model's own forbidden-vocabulary instruction). |
| `check:taxonomy` | 0 | 229 files clean (two pre-existing, non-failing "inert label" notes, unrelated to this round). |
| `check:nav-promise` | 0 | New gate; self-tested against both the real and a reintroduced defect. |
| `check:a11y` (4 sampled routes) | 0 | Covering both `p-as-heading` fixes and all three `focus-order-semantics` fixes; clean across 4 routes × 2 themes × 2 widths. |
| `check:yallo-case` | 0 | Exhaustive, 167 routes, ~31s. Found and closed 4 instances (1 known + 3 new). |
| `check:reflow` | 0 | Exhaustive, 167 routes, ~3m42s (was ~30s at 22 routes). |
| `check:gate-coverage` | 0 | 6 enumerating gates, all templates with a live URL covered by at least one. |
| `check:phase8 --passes 4` | 1 (both runs) | `check-phase8.mjs`'s own exit code for "gate not met" (reserves exit 2 for a hard run failure, which did not occur) — Lighthouse 90+ and LCP miss both before and after, exactly as reported in the tables above. Not a script failure. |

No gate is reported passing here that was not actually run against a live server with its real exit code observed.
