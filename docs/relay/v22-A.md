# Relay — v22-A · Round 14, Session A · integration, the pilot blocker, the honest gate

**4 August 2026 · feat/round14-foundations · HEAD `600201b`**

---

## What you did not do, and why

**`/intelligence/research` was not built.** Sumeet asked mid-round to check and fix this route ("the LTI report's Yallo version"). It genuinely does not exist — confirmed by repo-wide grep, nothing in `src/` references it. But it is not a small fix: `docs/design/context-round14-research.md` is the drafted brief (LTI licence ratified 3 Aug, methodology and findings drafted), and `context-round14-scope.md` §6 item 3 lists "the five research conclusions, drafted 4 August and awaiting his ruling" as still open. Building the route now would mean either shipping empty pages or writing the five pieces' conclusions myself — the second is a commercial-judgement call the standing brief explicitly reserves for Sumeet ("Chat drafts from the extract, Sumeet rules on the commercial judgement"). Flagged back rather than guessed. Exact unblocking question below.

**No CRM integration, no candidate-assistant variant, no admin UI for lead data, no biography/specialism/contact detail for any named person, no Lighthouse or `check:phase8` this round.** All per `context-round14-scope.md` §4, unchanged, not attempted.

## Retractions

None.

## Shipped

**Housekeeping (item 0), full sequence, `ROUND14-SPLIT-OK` printed:**
- `main` and `feat/round13-foundations` were **already** at `80e634d`/merged when this session started — not at the `158d8ad`/`03da3a3` the dispatch expected. Reported the real tips before touching anything, per the dispatch's own instruction. Checkout confirmed exclusive (no lock files, worktree B was clean).
- Merge verified rather than trusted: no conflict markers, `feat/round13-foundations` has zero diff vs `main` outside the merge itself, the assistant branch's only divergence from `main` is the accepted `AssistantMount.tsx` removal. `check:terms` ✅, `check:taxonomy` ✅, `tsc --noEmit` ✅, production build ✅ — all before item 1, as instructed.
- `feat/round13-assistant` (`3ceb588`) re-confirmed an ancestor of `main` (not of the pre-merge foundations tip) → deleted, `../yallo-talent-website-B` removed.
- New split cut from verified `main`: `../yallo-talent-website-B` on `feat/round14-surfaces`, this checkout on `feat/round14-foundations`. `session-a-r14` (3114, `.next-a-r14`) / `session-b-r14` (3214, `.next-b-r14`) added to `.claude/launch.json`.

**Item 1 — transcript persistence, the pilot blocker (`f5ac173`).** `assistant_transcripts`: new append-only table on the `submissions` Neon instance, not in the `submissions` family (§2.1's reasoning — a purgeable row type inside an append-only store risks the purge deleting a lead). `src/lib/db/migrations/0002_assistant_transcripts.sql`, `src/lib/db/transcripts.ts`. Every `/api/assistant/chat` turn now writes a snapshot of the running conversation (messages so far + this turn's reply) keyed by `transcriptId` — the newest row per id is the current transcript. Persistence failure logs and never fails the reply back to the visitor. **The retention purge shipped in the same commit, not later**: `scripts/purge-assistant-transcripts.mjs`, `db:purge-transcripts`, `.github/workflows/purge-transcripts.yml` (daily cron, 365-day window matching `TRANSCRIPT_RETENTION_DAYS`).

**Item 2 — `check-a11y.mjs`'s honesty gap (`34a1daf`).** The docstring claimed "every surface" while sampling 6 hand-picked routes, unregistered in `check-gate-coverage.mjs`. All six constraints in §2.2 held together — no binding-pair conflict to report:
- `scripts/lib/rendering-units.mjs` extracts the shell-derivation logic `check-gate-coverage.mjs` already had, so both scripts share one implementation.
- Default (PR-gate) run: `sampleOnePerShell()` — one live URL per distinct rendering unit, derived from the sitemap, never a hand list.
- `--exhaustive`: every published URL, on a new daily cron (`.github/workflows/a11y-exhaustive.yml`), never the PR trigger.
- Docstring rewritten to state what the default run actually visits and why (unit coverage, not URL coverage).
- Registered in `check-gate-coverage.mjs`, which computes the identical sample to credit coverage — the claim can't drift from what the default run visits, because it's the same function.
- **Measured**: 21/21 reachable rendering units (`check-gate-coverage.mjs http://localhost:3114`), axe clean in both themes/widths, **2:30 against a production build** (`next build && next start`, matching how the PR gate actually runs). A first measurement against `next dev` read 3:06/3:07 — cold Turbopack per-route compilation, not a PR-gate condition — logged here so a future session doesn't re-measure the wrong thing and conclude a conflict that isn't real.

**Item 3 — `submissions` read path (`f5ac173`, same commit as item 1).** `scripts/db-read-submissions.mjs`, `db:read-submissions` — lists recent rows with delivery status. No admin route, no auth surface, no page, per §2.6's explicit constraint.

**Three ad hoc fixes from Sumeet mid-round (`600201b`), outside the dispatch's scope:**
1. Evidence mega-menu's featured case swapped from Al Tayer to the Alshaya custom-planning-platform case study (`src/components/layout/nav-config.ts`) — copy trimmed from the case study's real excerpt, not paraphrased.
2. Hover-only gold arrow added to linked H2/H3 headings that had no clickability cue: `.vow h3 > a` (intelligence, blueprint index, /ai-talent — scoped to skip `.moduleLink` anchors, which already carry their own arrow) and `a.card .cardTitle` (insights, about's engagement-type cards — scoped to the anchor variant, so leadership/jobs/why-yallo's identically-classed static cards are untouched). Verified in-browser: arrow shows only on hover, only on the hovered card; axe re-run clean, no new contrast finding from the gold glyph.

## Decisions taken under delegated authority

- **Persistence model for transcripts: append-only snapshot per turn, not update-in-place.** Reversible — it's a table design, not a migration that touches existing data.
- **Items 1 and 3 committed together** (both small, both `package.json`/`src/lib/db`/`scripts` changes). Trivially reversible via `git revert` or a later split if review wants them apart.
- **The three ad hoc fixes made directly in this worktree**, in `src/components/**` — B's declared territory this round — rather than deferred, because B's worktree had not been cut yet when Sumeet asked (no live session on those files to collide with). See risk below.

## What B needs to know

`feat/round14-surfaces` was cut from `main` **before** commit `600201b` landed, so B's branch does not have the featured-case swap or the two hover-arrow CSS additions. Recommend B rebase onto or merge `feat/round14-foundations` (pushed, `origin/feat/round14-foundations`) before starting work — otherwise B's later merge will show these as B-side conflicts against files B never touched. Nothing else needed: `check-a11y.mjs`'s rewrite is read-only from B's side per §3.1 item 3, and none of round 14's pre-adjudicated seams changed.

## Anything needed from B's territory

The three ad hoc fixes above touched `src/components/layout/nav-config.ts`, `src/components/blocks/home/Home.module.css`, `src/components/blocks/editorial/EditorialLayout.module.css` — all B's per §3. Logged here rather than left silent, per the adjacent-fix policy's spirit even though this wasn't a gate-surfaced defect. No other territory need.

## Open items, each with its unblocking question

1. **`/intelligence/research`.** Unblocking question: does Sumeet ratify the five research conclusions in `context-round14-research.md` now, and confirm who writes the published analysis (Chat drafts, per the brief) — so this becomes a dispatchable item rather than an assumption?
2. **GitHub reports one high-severity Dependabot alert on `main`** (surfaced on push, not investigated — outside this round's scope and not something to triage unprompted). Unblocking question: does Sumeet want this triaged this round, or logged for its own pass?
3. Carried, unchanged from `context-round14-scope.md` §6: the Phase 8 go-live decision, metric definitions, the five research conclusions (same as item 1 above), specialism/contact route per named leader, Informatica classification, R-AI3 amendment veto window, go-live date pending Raphy.

## Risks

- **B-side conflict risk on the three ad hoc files**, mitigated by the rebase note above — real only if B starts from the stale worktree without pulling first.
- **`scripts/purge-assistant-transcripts.mjs` and the two new cron workflows are unverified against a live database** — no `DATABASE_URL` available in this session. Syntax-checked (`node --check`) and reasoned through against the same query shape `transcripts.ts` uses, but the first real run should be watched, not assumed green.

## HEAD and gate exit codes, this session

HEAD: `600201b` (pushed, tracking `origin/feat/round14-foundations`).

| Gate | Exit | Notes |
|---|---|---|
| `check:terms` | 0 | ×3 runs, clean throughout |
| `check:taxonomy` | 0 | ×2 runs, same 2 pre-existing dead-copy advisories, not failures |
| `tsc --noEmit` | 0 | ×3 runs |
| `pnpm build` (production) | 0 | ×3 runs (housekeeping, timing measurement, final) |
| `check-gate-coverage.mjs` | 0 | 21/21 reachable units, 0 problems, ×2 runs |
| `check:a11y` (default) | 0 | Clean, no serious/critical violations, both before and after the UI fixes |
