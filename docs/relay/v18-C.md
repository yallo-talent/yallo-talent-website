# Relay — v18.0, Session C: SEO, retrieval and CI

**3 August 2026 · `feat/round10-seo` · HEAD `54506e0`**

Ground confirmed clean at open: both round 9 branches merged into `fd459bc`, working tree clean, Node 22 / pnpm 10 matched (no drift to fix). Built with `NEXT_DIST_DIR=.next-c-r10`, served on port 3307 throughout, per this session's assignment.

---

## What shipped

Six commits, `f5fba58` through `54506e0`. All six items in the brief closed.

1. **`f5fba58`** — Environment-driven crawler policy, the actual gap closed. `robots.ts` and `buildMetadata`'s per-page `robots` field already correctly gated on `isProductionHost` (built in round 7, already on this branch) — that part of B6 was already done before this session started. The real gap: the homepage (`src/app/page.tsx`) has no metadata export of its own and inherits only the root layout's static `metadata`, which carries no `robots` field at all. On a non-production host the highest-traffic page defaulted to indexable with zero signal. Fixed with `src/middleware.ts` setting `X-Robots-Tag: noindex, nofollow` on every response when `!isProductionHost` — covers any page regardless of whether it calls `buildMetadata`, rather than requiring per-page opt-in. Proved both directions: built with `NEXT_PUBLIC_SITE_URL` unset (homepage carries the header, `robots.txt` disallows all three families) and with it set to `https://yallo.co` (no header, `robots.txt` allows, meta reads `index, follow`).

2. **`3998da3`** — Wired `check:taxonomy`, `check:yallo-case`, `check:estate`, `check:marks`, `check:cs-excerpts` and `check:gate-coverage` into the existing PR job. Proved each can fail with the exact CI invocation, then reverted:
   - `check:taxonomy`: added `"Data & AI"` to a discipline data file, watched it name the line, reverted.
   - `check:cs-excerpts`: `--selftest` already proves this against a fixture; added it as a CI step ahead of the real corpus.
   - `check:yallo-case`: set literal `"YALLO"` in rendered copy, watched it name the element and cause, reverted.
   - `check:estate`: removed the `data-lit` wiring, watched keyboard focus light nothing, reverted.
   - `check:marks`: inflated one asset's declared ink fraction in the manifest, watched it fall 32.6% outside a 2% tolerance, reverted.
   - `check:gate-coverage`: dropped one route from `check-reflow`'s list, watched it report zero coverage for that shell, reverted.
   `check:crawlers` is different in kind — it probes the live production and placeholder hosts over the real network, infrastructure this repo does not control and which is not yet running this build (pre-cutover, still WordPress). Gating merges on an external site's availability would fail PRs for reasons unrelated to the diff, so it runs on its own daily schedule in `.github/workflows/crawler-access.yml` plus `workflow_dispatch`. Proved its fail path against a local UA-discriminating fixture (not a real external host): pointed `PROBE_PRODUCTION_URL` at it, watched it name a blocked crawler and fail. **Not run inside an actual GitHub Actions job as part of this proof** — that needs a push or PR round-trip against real infrastructure, which this session did not do. What's verified is the script and the exact CI invocation, not a live Actions run.

3. **`81da713`** — New gate: `scripts/check-no-redirect-hops.mjs` / `pnpm check:no-redirects`. `check:yallo-case` already proves every internal href eventually resolves (follows redirects, fails only on 404/no-response) — a working redirect chain passes it today. This gate reads `/sitemap.xml`, fetches every published page, collects internal hrefs via cheerio, and probes each with `redirect: "manual"` so a 3xx surfaces as itself. Proved it fails: linked to `/insights/septembers-biggest-shifts` (a real legacy slug `next.config.ts` 308s to `/industries/retail`), rebuilt, watched it name the source page, status and destination, reverted. Passes clean at 231 distinct hrefs across 167 pages.

4. **`4c7ae24`** — Per-page OG images. `SITE.defaultOgImage` pointed at `/images/og-default.jpg`, which never existed — confirmed with `ls`, confirmed no page had ever set its own `seo.ogImage` either, so every share on every channel previewed broken. `src/app/og/[[...slug]]/route.tsx` generates a 1200×630 PNG per published route with `generateStaticParams` (prerendered at build time — confirmed `●` in the build output, not `ƒ`). Reuses `hashSeed` from `petal-geometry.ts` so a page's OG card and its own PetalPlate surfaces are seeded identically. Satori (next/og's renderer) has no SVG path support and never resolves `var(--amb)`, so the petal shape is expressed as a div with one corner's border-radius set instead, and colours come from `src/lib/og-palette.ts`, which parses `globals.css` Layer 1 directly rather than hand-copying hex values — the pre-commit colour hook would have blocked a literal hex in any `.tsx`/`.ts` file, and this avoids that by construction. No text in the card at all, so no font ever needed embedding. Confirmed the response is a genuine 1200×630 PNG and `og:image`/`twitter:image` both resolve on a real page.

   Extracted `src/lib/published-routes.ts` out of `sitemap.ts`'s inline route-building so the OG route's `generateStaticParams`, `sitemap.ts` and `llms.txt` share one enumeration. Verified the refactored `sitemap.ts` byte-identical to the original (`lastmod` timestamps aside) before removing the old inline version.

   **Known gap, not mine to close**: the homepage still has no `og:image` — same root cause as item 1, no metadata export of its own, and the root layout's static metadata has no `openGraph` field. Fixing it means editing `src/app/page.tsx` or `src/app/layout.tsx`, B's territory this round.

5. **Organization schema — verified, not rebuilt.** `src/lib/jsonld.ts` already existed on this branch (round 7, commit `79ae8f2` and earlier) with `organisationJsonLd()` covering all four entities, "Bengaluru" correctly (never "Bangalore"), platforms derived from `platformLabels()` rather than hand-typed, no `Person` schema. Confirmed it renders as valid JSON-LD on the homepage and the referenced logo file actually exists and serves. **The one real gap — `sameAs` — is not built**, and I did not fabricate it: there is no LinkedIn company URL or any other owned-profile URL anywhere in this repository as a ratified fact, and inventing one would be exactly the kind of claim canon forbids. See open items below.

6. **`54506e0`** — `llms.txt` at `src/app/llms.txt/route.ts`, generated from the same `publishedPaths()` function alongside `sitemap.ts`, filtered to published routes (167 links, matching the sitemap's 167 `<loc>` entries exactly). Labels are mechanically humanised from each path's own last segment rather than hand-typed, so the file can't assert anything the URL doesn't already say — a few acronym labels look odd ("Sap", "Eor", "Crm") as the cost of zero invented copy. Not described anywhere, including here, as an AI-visibility or citation lever.

---

## Decisions made under delegated authority

- **`check:crawlers` moved to a scheduled workflow rather than the PR gate.** Reversible (moving it back is a one-line change), but I'm flagging it because the brief said "wire the seven … into CI" without specifying per-PR vs. scheduled. Gating merges on a live external site pre-cutover seemed clearly wrong; happy to be overruled.
- **Refactored `sitemap.ts`'s internals** to extract `publishedPaths()`. In scope (I own `sitemap.ts`), verified byte-identical output before treating it as safe.
- **Did not touch `layout.tsx` or `page.tsx`** to close the homepage's `og:image`/robots-meta gaps at the metadata level, using middleware instead for the robots half and leaving the OG half as a logged gap. Both are B's files this round.

## What I did not do, and why

- **`Organization` `sameAs`** — no ratified LinkedIn or owned-profile URL exists anywhere in this repo. Not built; see open items.
- **`check:crawlers` inside an actual GitHub Actions run** — proved the script and the exact invocation locally against a fixture; did not push or open a PR to watch a real Actions job go red, since that means round-tripping against real infrastructure from this session.
- **Nothing outside my territory.** No edits to `src/app/**` pages, `src/data/**`, or `src/components/**`, beyond the two new generated routes (`og`, `llms.txt`) and `middleware.ts`, all of which are new files, not edits to existing ones.

## Open items, with the exact unblocking question

1. **Does Yallo Talent have a ratified LinkedIn company page (or any other owned social profile) to list in `Organization.sameAs`?** If yes, the URL(s). Without this, `sameAs` stays unbuilt rather than fabricated.
2. **`check:crawlers` in CI**: confirm the daily-schedule-not-PR-gate placement is the right call, or say if it should also gate PRs once the site is live on `yallo.co`.

## Risks

- **`check:crawlers`'s daily schedule** means a Cloudflare misconfiguration blocking retrieval bots could sit undetected for up to 24 hours. Acceptable given the alternative (flaky PR gates on external state), but worth knowing.
- **The homepage's missing `og:image` and (already logged in round 9 territory) missing `openGraph` field** persist until B adds a `metadata` export to `src/app/page.tsx`. Every other page is fixed; the homepage — the highest-traffic page — is not, for either the OG card or (structurally, if B ever touches it) any future per-page metadata.

## HEAD and real exit codes

HEAD: `54506e0`. `pnpm build` exit 0 (clean, from a fully deleted `.next-c-r10`). `npx tsc --noEmit` exit 0. `npx eslint src scripts`: 35 pre-existing errors / 13 warnings, all pre-dating this session (`NavBar.tsx`/`ThemeToggle.tsx` `setState`-in-effect, `derive.ts` `module` assignment — both explicitly out of scope per `context-round10-scope.md` §1) — nothing new from this session's files. All seven owned gates plus the existing suite re-run clean on the final build, port 3307: `check:taxonomy`, `check:cs-excerpts`, `check:yallo-case`, `check:estate`, `check:marks`, `check:gate-coverage`, `check:no-redirects` — all exit 0.

Working tree clean. Not merged to `main`.

## Notes for A and B

- **For B**: `src/app/page.tsx` has no `metadata` export and inherits only the root layout's static one, which has neither a `robots` field nor an `openGraph` field. I closed the robots half with middleware (environment-wide, not page-specific), but the OG image half is still open — the homepage won't get its `/og` card until it has its own `buildMetadata()` call, same as every other page in this codebase already does.
- **For A**: nothing touched in your territory (`scripts/**` outside gates I added, `src/components/**`, `src/styles/**`, `globals.css`, `nav-config.ts`). `.github/workflows/ci.yml` gained six new steps and one new file (`crawler-access.yml`) — flagging in case either affects a Phase 8 gate run you're timing.
- **Both**: `src/lib/routes.ts` already exists (a `routeExists()` utility, unrelated to anything I built) — I named my new file `src/lib/published-routes.ts` specifically to avoid colliding with it. Worth knowing if either of you reaches for a "routes" module by guessed name.
