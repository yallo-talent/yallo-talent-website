# Code → Chat relay v19.0 — Round 11, Session B

**Branch:** `feat/round11-pages`, this worktree (`../yallo-talent-website-B`)
· **Dist dir:** `.next-b-r11` · **Port:** 3207 · **Base at open:**
`56fb7fc` (round 10's third and last merge commit — chrome, pages and SEO
all present), confirmed via `git log --oneline -4` before any work
started, working tree clean.

---

## 1. Where this stopped

All three assigned items closed: the leadership roster (§2), the founder
restoration on `/about` (§2.6) and the homepage metadata export (§4.3).
Nothing else in `context-round11-scope.md` §4 was in scope for this
session. `/leadership` and `/about` are round 10 page-groups reopened only
for the defects this work surfaced (§4.6) — no full close-out loop was
re-run across either page.

**HEAD:** `7b9a7b4d1a6bd7dc543198d1de2e904f32480281`

```
7b9a7b4 feat(seo): add the missing metadata export to the homepage
4ee3bf7 fix(about): restore the founder's name to the founding timeline
d00d98d feat(leadership): name the ratified five-person roster
56fb7fc chore(merge): round 10 SEO, crawler policy, CI gates and OG images
```

Three commits, in dependency order. `tsconfig.json`'s `next build`/`next
start` rewrite (adding `.next-b-r11/types/**` to `include`) was reverted
with `git checkout -- tsconfig.json` before every stage, per AGENTS.md;
never staged, never committed. Everything staged by explicit path — no
`git add -A` anywhere in this session.

---

## 2. The leadership roster (§2)

**One index, `src/data/team/index.ts`**, `TeamMember { slug, name, role,
linkedin?, bio? }`. `/leadership` maps over it — no hand-typed sixth
roster. Five entries, in the order Sumeet ratified:

| Display name | Role | LinkedIn | Bio |
|---|---|---|---|
| Sumeet Goenka | Founder & CEO | *(none supplied)* | Yes — see below |
| Chandrashekhar Kolar | Head of Managed Delivery | `.../chandrasekharkolar/` | *(absent)* |
| Niharika Patir | Head of Talent Operations | `.../patirniharika/` | *(absent)* |
| Raphy Varghese | Head of Marketing & Growth | `.../vargheseraphy/` | *(absent)* |
| Kritika Poddar | Head of Finance & PMO | `.../kritikapo/` | *(absent)* |

`bio` and `linkedin` are both **optional fields, absent for the four**
where not supplied — never an empty string, never a dash, never "bio to
follow." Confirmed by reading the rendered accessibility tree: no bio
node exists under any of the four cards.

**Decision under delegated authority — the name spelling (§2.3).**
Sumeet corrected the display spelling live, mid-session, to
**"Chandrashekhar Kolar"**, superseding the scope doc's own transcription
("Chandrashekar"). Applied as the display name. The LinkedIn slug is
untouched — `chandrasekharkolar`, exactly as originally supplied — a
third transliteration, different again from both the doc's and the live
correction. Flagged in code, not reconciled, per §2.3's own rule. This is
reversible (a one-line data edit) but is a real content decision, so
naming it here rather than treating it as a typo fix.

**Sumeet's bio** — the only one shipped, and the only place §2.1 permits
one: "Enterprise IT operator at Richemont, retail transformation lead at
Landmark Group and programme director at Alshaya EMEA, before founding
Yallo Talent. The operators who shipped those programmes are the ones
who review every shortlist today." Every clause traces to a sentence
already live on `/why-yallo` (its credentials section's three org/role
pairs, and its hero lede's "the same operators who shipped enterprise
programmes at Richemont, Landmark and Alshaya EMEA are the ones
reviewing every shortlist you get"). Nothing new: no tenure figure, no
date, no capability claim beyond what those two sources already assert.

**No photographs, silhouettes, monograms or PetalPlate-as-portrait.** The
data shape has no `image` field at all, so the round can't drift into one
by omission.

**Honesty fix surfaced by the roster change, not part of the original
ask:** the section heading directly above the cards read "Six operators.
Six practices." with a lede claiming "Each practice lead has been in the
role they screen for" — true of the retired anonymous six-practice-lead
array, false of the five named functions now shown (Marketing & Growth
and Finance & PMO don't screen shortlists). Rewrote the heading/lede to
state only what the roster itself shows, and reused `/about`'s own
"Specialist-led, always" sentence verbatim for the adjacent philosophy
card that had the same "practice lead" mismatch, plus one word-level fix
to the bottom CTA sub-line for the same reason. No new claim introduced
anywhere; all replacement text is either pure enumeration of the roster's
own fields or copy already published elsewhere on the site.

**Verification run, both themes, 360–1440:** `check-terminology.mjs`
clean (247 files), `tsc --noEmit` clean, `eslint` clean on every touched
file, `check-a11y.mjs --routes /,/leadership,/about` clean across 3
routes × 2 themes × 2 widths (one pre-existing, unrelated exemption:
`__ghost` numerals), `check-rendered-type.mjs` clean, `check-reflow.mjs`
clean (no horizontal overflow at 320/360). `check-gate-coverage.mjs`
confirms every rendering unit with a live URL is still visited by at
least one gate — `/leadership` was already only covered by `check-reflow`
among the six enumerating gates before this round, which is a pre-existing
shape in `scripts/**` and not something this session touched or could
touch.

---

## 3. LinkedIn URL verification — attempted, inconclusive, not a blocker

§4.1 asks each URL to be confirmed resolving before shipping. Attempted
via `curl` (with a browser user-agent) and via the Browser pane's real
Chromium: `curl` returned LinkedIn's standard anti-automation `999` for
three of the four and `200` for one; the Chromium navigation to a
LinkedIn profile timed out and never left the origin, consistent with
LinkedIn actively blocking automated browser traffic rather than the
profile being invalid. **I cannot get a reliable machine verification of
these four URLs** — LinkedIn's bot defences make this specific check
unautomatable from this session, not something I chose to skip. The URLs
shipped are exactly what Sumeet supplied, unmodified, unguessed. **Open
item: a human, logged into LinkedIn in a real browser, should open the
four links once before this page goes live.**

---

## 4. `/about` — founder restored (§2.6)

One line, `src/app/about/page.tsx`, timeline entry `year: "Founded"`:
restored to `"Sumeet Goenka and a small team of enterprise operators
start Yallo Talent to fix the volume-over-fit problem in enterprise
hiring."` — the exact string round 10 removed, confirmed via `git log -p`
against this file before restoring, not retyped from memory.

Pre-existing, unrelated: four `react/no-unescaped-entities` eslint
findings on lines 83, 153, 241, 289 of this same file, none on lines this
session touched. Logged, not fixed — out of the one-line scope of this
change and not something this session's edit created.

---

## 5. Homepage metadata export (§4.3)

`src/app/page.tsx` had no `metadata` export at all and inherited only the
root layout's static title/description — no `robots`, no `openGraph`, and
`/` was the one route on the site previewing with no OG card. Added a
`buildMetadata()` call, title and description carried over unchanged from
`layout.tsx`'s existing copy (no new claim written), `path: "/"`.

**Confirmed, not assumed:** `curl -I http://localhost:3207/og` returns
`content-type: image/png`; downloaded and read the PNG header directly —
`1200 x 630`, exact. Rendered `<head>` on `/` now carries `og:image`,
`og:image:width/height/alt` and `twitter:image`, matching the shape every
other page already has (checked `/leadership`'s `<head>` side by side —
identical pattern, both resolving through the same
`http://localhost:3000/...` base from `NEXT_PUBLIC_SITE_URL`'s
unset-locally default, which is pre-existing sitewide behaviour and not
something this change introduced).

**No hero copy was touched.** `p.Home-module__heroLede` — the homepage's
LCP element per A's Phase 8 measurement — is unchanged. **For A:** no
hero copy changed anywhere on `/`, so nothing on this page should move
the Phase 8 numbers.

---

## 6. What I did not do, and why

- **No `Organization.sameAs`.** §4.5 is explicit: the five LinkedIn URLs
  Sumeet supplied are personal profiles, and `sameAs` needs the company
  page. Not built, not guessed, not substituted. Still open — see below.
- **No `Person` schema.** Deferred per §2.5, explicitly out of this
  round.
- **No photographs.** Not attempted, not requested from Sumeet, no
  scraping of LinkedIn for images (explicitly banned, §2.2).
- **No touching `src/components/**`, styles, `nav-config.ts`,
  `scripts/**`, workflows or `package.json`.** Confirmed by final `git
  status` before each commit — only `src/app/**` and `src/data/**` paths
  staged.
- **Did not re-run the round 9/10 close-out loop on `/leadership` or
  `/about`.** Only fixed what this round's own changes surfaced, per
  §4.6.

---

## 7. Open items for Sumeet (carried and new)

1. **The Yallo Talent company LinkedIn page URL**, for
   `Organization.sameAs`. Still blocked — §4.5.
2. **Four short bios** — Chandrashekhar Kolar, Niharika Patir, Raphy
   Varghese, Kritika Poddar. Each is one edit to `src/data/team/index.ts`
   (add a `bio` field) whenever Sumeet supplies the text. Nothing else on
   the page needs to change; the roster already renders conditionally.
3. **A human check of the four LinkedIn URLs** in a real, logged-in
   browser — see §3. Not a code change, a five-minute manual check.
4. **Monochrome team portraits + the photography canon amendment**
   (§2.2) — unchanged from the scope doc, still needs Sumeet's
   ratification and a `public/team/**`-scoped CI carve-out before any
   image work starts.
5. Carried, unchanged from round 10 §5: client logo consent, metric
   definitions, the Academy "Launching" footer marker, `ThemeToggle`
   mount decision, the font budget (if A's Phase 8 gate stops short),
   go-live date.

---

## 8. Risks

- The LinkedIn slug mismatch (§2.3) is now flagged in three places — the
  data file comment, this relay, and open item 4 — but is genuinely live
  on the site as shipped: two different transliterations of one name,
  side by side (display text vs. URL path). Low risk (both are
  real, Sumeet-supplied strings) but worth Sumeet seeing once before
  wide circulation.
- `/leadership`'s section-heading and CTA copy fixes (§2, "practice
  lead") are a judgement call under delegated authority: real, but a
  content decision rather than a mechanical one. Reversible in one file.

---

## 9. Notes for A

- **No hero copy changed on `/`** — see §5. Nothing here should move the
  Phase 8 numbers; if `/` moves anyway, the cause is elsewhere.
- **`/leadership` and `/about` were touched** (§2, §2.6), both inside my
  territory (`src/app/**`), no components or styles edited. If your
  merge of the three round 10 branches already settled before this
  session opened (it had — `56fb7fc` confirmed at start), there is no
  overlap to reconcile; these are new commits on top, not conflicting
  edits to shared files.
- `check-gate-coverage.mjs` still shows `/leadership` covered by only one
  of your six enumerating gates (`check-reflow`) — pre-existing, not
  changed by this session, noted in case it's useful context for the
  gate-coverage work.
