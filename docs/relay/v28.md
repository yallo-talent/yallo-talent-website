# Relay v28 — round 20

**7 August 2026 · Code lens · main repo, single session, port 3115 · HEAD `15c6b99`**

Scope: `docs/design/context-round20-scope.md`. Readiness report:
[`docs/status/CUTOVER-READINESS.md`](../status/CUTOVER-READINESS.md).

---

## 1. Shipped

**The 2px is fixed, in one push, not three.** `.bar > * { min-width: 0 }`. A flex
item defaults to `min-width: auto`, a floor at its own content width, so the
bar's children refused to shrink and the surplus left the page instead of the
row. Three previous fixes were subtractions tuned to one machine's font metrics:
locally the row cleared 360 with 3.4px spare, CI's faces render it 5.4px wider.
Measured after: `over=0` at 320, 360 and 390, with the webfonts loaded and again
with them blocked so the row lays out on wider system faces. Declared on the
child set, so a fourth child inherits it. **CI on `07db8e2` went green** — the
first fully green CI in this repository's recent history.

**Contrast: all three findings closed, no wash touched.** The service hero paints
its own ambient instead of composing `.amb-wash`, so it never inherited the half
of that class which steps `--text-3` to the wash grade: eyebrow 4.45:1 and
audience list 4.34:1, now above 5:1. The L2 hero read `--accent-line` (the ≥3:1
grade) as 62px text over a photographic plate: 2.75:1, now 3.99:1 at the label
grade. Both as scoped context tokens; dark theme is a no-op in both.
`check:contrast-render` exit 0 over 13 routes × 2 themes, 512 text runs.

**The watched commit — the whole round trip, and it landed.** Branch
`admin/2026-08-07T07-29-20-906Z` created, `order.yaml` committed, **PR #13**
opened, diff exactly the two lines that swapped, **CI passed**, **merged to
`main` as `66a5a91`**, and **rendered**: built from merged `main`,
`/case-studies` lists Al Tayer first and Majid Al Futtaim second, which is the
swap the pane made. Every stage observed.

One precision that must not be lost in the retelling. **Auto-merge did not
perform the merge.** GitHub refused it — `Pull request is in unstable status` —
because `main` has no required status check, so nothing blocks the merge and
auto-merge has nothing to queue behind. The module reported that and stopped, as
it must. Sumeet then authorised the merge explicitly and it was performed with
`gh pr merge`. So: the cockpit branches, commits and opens a mergeable pull
request, watched end to end. It does not yet publish unattended.

**Case-study lifecycle.** Edit, unpublish and a publish log, all on the one PR
path. Validation runs before any PR opens, importing the build's own schema.
Unpublish is `published: false`, never a deletion, written as a single scalar so
the diff is one line. `check:write-path` extended to 47 assertions and **proved
red first** on the order.yaml-preservation invariant.

**Transcripts v2 and the Articles shell.** Summary list default, deterministic
from stored fields, filters as a GET form, detail one click deeper. Articles is a
disabled pane that says where articles live.

**Purge workflow fixed and verified.** Diagnosed: `DATABASE_URL:` was empty in
the 05:01 run env. Sumeet's secret landed at 06:50; a dispatch at 07:12 concluded
**success**.

**CI wiring.** `workflow_dispatch`; `check:write-path`, `check:metrics` and
`check:admin-render` added, the last behind throwaway secrets generated in this
session and used nowhere else.

**Four things Sumeet asked for mid-round**, each measured before and after:
the two Intelligence CTAs (dead text over live routes), the assistant drawer's
bottom-left corner plus the composer padding the corner needed, and the hero
eyebrow dropping India so it stops wrapping.

---

## 2. Not reached

Nothing in the scope file was skipped. Two things inside it are incomplete for
reasons outside this session's reach, both in §5 below.

---

## 3. Delegated decisions, with reversibility

| Decision | Why | To reverse |
|---|---|---|
| `repoSlugFrom` accepts an https or ssh remote, not only `owner/name` | `ADMIN_GITHUB_REPO` held the full URL — what `git remote -v` prints, and what the step list said to copy. The old test would have failed the first publish and blamed a variable that was correct | Restore the single regex |
| `serialiseOrder` preserves the existing header and per-slug comments | The shipped `order.yaml` carries 22 lines of provenance and a client name per entry; the old serialiser deleted all of it on the first reorder. Reversible in position, not in prose | Drop the `previous` argument |
| The edit form is a client component using `useActionState` | Errors return in the response instead of a kilobyte of prose per error in the URL, and the operator's draft survives an invalid save | Revert to a redirect with encoded errors |
| `check:assistant-links` runs conditionally and reports itself absent | It needs `ANTHROPIC_API_KEY`, which §7.5 did not ratify and which is charged per run | Delete the `if:` to make it blocking |
| CI retires the server before the browser gates | Four runs failed at the a11y step on a wedged next/image optimiser that a retry could not survive. A new process is the only recovery | Delete the `Fresh server` step |
| `check-admin-render` discovers the conversation-detail URL | Its address is a transcript id, so it cannot be hardcoded. Reports the template unvisited when the list is empty | Hardcode an id |
| Hero eyebrow scoped to that line only | Sumeet's instruction was explicit about scope; India still appears twelve times in the homepage markup | One-word revert |

---

## 4. Not done, and why

- **Auto-merge still does not land a cockpit publish**, and this is now the only
  thing left. `allow_auto_merge` is true; the refusal is `Pull request is in
  unstable status`, because `main` carries no required status check. The fix is
  branch protection, and **the agent was refused permission to write it** — a
  repository security setting is outside what it may change. Sumeet's, one
  setting: require the `checks` status check on `main`, administrators excluded so
  direct pushes keep working.
- **Page of origin is recorded for 1 of 94 conversations.**
  `assistant_transcripts` holds `transcript_id`, `messages`, `created_at` and
  nothing else, and the panel sends no path, so the only origin in the database
  sits on `submissions`. Reported as absent rather than inferred.
- **`check:assistant-links` did not run this round** and cannot run in CI.
- **Phase 8 cannot be closed from here** — it needs the production host.
- **Phase 8 performance gate is red** — see §7.

---

## 5. Retractions

**Required, and not empty this round.**

1. **"The re-run failed identically, so it is reproducible, not flaky."** Wrong,
   and I said it to Sumeet before the control finished. Re-running CI unchanged
   on `07db8e2` — already green that morning — failed at the same step with the
   same message. The a11y timeout is not attributable to this round's commits.
2. **The session-drop diagnosis was wrong twice over.** I reported that the long
   validation URL was evicting the Auth.js session cookie, and wrote that
   explanation into two source comments as a measurement. It was neither: my
   Playwright selector `button[type="submit"]` was matching the layout's **Sign
   out** button, which is the first submit control in the DOM. The app was
   correct throughout. Both comments are corrected in the commits that carry
   them. The `useActionState` refactor was kept — it is still the right shape, for
   the reasons now stated — but it was reached through a false diagnosis and the
   record should say so.
3. **A commit message said the assistant drawer's corner was scoped to
   "601px-and-up".** It is 641px. Amended before the push.
4. **The a11y retry was the wrong fix, and I shipped it before the evidence was
   in.** The next run disproved it: the second navigation timed out too, which
   means the server was wedged rather than slow. Removed, and replaced with the
   cause fix — retiring that server before the browser gates. That is what turned
   the step green.
5. **CI ran nothing at all on `1f910c8`.** I read the `secrets` context from a
   step-level `if:`, which is not a place that context is available, so GitHub
   rejected the workflow file. Python's YAML parser accepted it, which proved
   nothing. Fixed in `722a15e`.
6. **The first "fresh server" CI step passed while doing nothing, and I let a
   green run stand on it.** `next start` becomes `next-server`, so
   `pkill -f "next start"` killed the wrapper and left the listener; the
   replacement never bound and the curl probe passed against the wedged server it
   was meant to replace. `8890f89` went green on that step by race, which is
   worse than failing. Fixed in `0b6e366`, which kills the right process and
   waits for the port to close.
7. **I reported CI green "on `0b6e366` and on the merge commit `66a5a91`".**
   Only `0b6e366` was verified at the time. `66a5a91`'s run was still queued and
   I inferred it from the parent, which is the exact move this round's discipline
   forbids. **The inference later proved correct** — `66a5a91` did conclude green
   — and the retraction stands anyway, because a guess that happens to be right
   is still a guess. All five runs have since been watched to conclusion.
8. **I pushed `722a15e` with the em-dash fix still in the working tree.** The gate
   was run, the fix was made, and the commit went out without it. CI caught it.

---

## 6. Open items, each with the question that unblocks it

Sumeet delegated these at the close of the round: decide unless something is
genuinely bad, in which case Chat decides. Decisions taken, with reasoning:

1. **Branch protection on `main` — NOT DECIDED, and not decidable here.** Writing
   branch protection is a repository security setting and was refused to the
   agent. **This is the one item that returns to Sumeet.**

   Re-checked at final HEAD, and the state moved during the round: protection
   returned 404 "Branch not protected" at ground and returns a protection object
   at the close. It still does not do the job — `contexts: []`, `checks: []`,
   `enforce_admins: false`. A rule requiring no check leaves the merge unblocked,
   so auto-merge still has nothing to queue behind and will still refuse. **The
   remaining step is one field: add `checks` to that rule's required status
   checks.** Read the state with an admin-capable credential, not
   `ADMIN_GITHUB_TOKEN` — it is fine-grained without Administration read and
   returns 403, which reads as "protected" if taken at face value.
2. **PR #13 — MERGED**, on his explicit instruction that auto-merge was okayed and
   should be honoured. Squash, branch deleted, verified on `main` and rendered.
3. **`ANTHROPIC_API_KEY` — DECIDED: leave unset.** It is his key and it is charged
   per CI run. `check:assistant-links` still runs locally, and every CI log prints
   a warning naming the gate and its owner, so nothing is hidden. Reversible by
   adding the secret; the step needs no other change.
4. **Page of origin on transcripts — DECIDED: leave partial, and this one is
   flagged for Chat rather than settled.** Recording it means a migration plus a
   new field captured from every visitor's browser, including those who submit
   nothing. That is a change to what the site collects about people, not an
   engineering choice, so the conservative option was taken and the decision is
   left open deliberately.
5. **Phase 8 — DECIDED: accept red, measure on the production host at cutover.**
   It cannot be closed from here and must not be closed by moving the threshold.
   Raphy dependency.

---

## 7. Risks

- **The performance gate is genuinely red and has never been measured where it
  matters.** Mobile 86–96 against a floor of 90, LCP 2.67–4.22s against a ceiling
  of 2.5s, measured on a laptop serving `next start` over loopback. On Vercel with
  a CDN these are different numbers. Nobody knows which way.
- **The a11y CI flake is not understood, only survived.** The retry makes runs
  pass; it does not explain why the first `load` after `check:visual` stalls. If
  it becomes routine the retry line will say so.
- **The cockpit's write path has been watched as far as an open pull request and
  no further.** Nothing has landed on `main` from it. That claim should not grow
  in the retelling.
- **`delete_branch_on_merge` is false**, so every cockpit publish leaves an
  `admin/` branch behind. Harmless, and it will accumulate.

---

## 8. Factual concern, stated once, after the fact (R-A9 carve-out)

None on published figures. `check:metrics` exits 0 and the 42 prose figures were
reported, never rewritten.

One observation, not an objection: the homepage's four-entity strip, footer and
corridor copy still carry India, and the eyebrow no longer does. That is exactly
what was instructed and it is internally consistent; it is noted only so the next
round does not read the eyebrow as a positioning change and propagate it.

---

## 9. HEAD, and every gate's real exit code

**HEAD `15c6b99`.**

Configuration re-verified against the live API at final HEAD, not only at ground:
`ADMIN_GITHUB_TOKEN` non-empty (93 chars), `ADMIN_GITHUB_REPO` normalising to
`yallo-talent/yallo-talent-website` and matching `git remote -v` with a 200 from
the API, `allow_auto_merge: true`. Full block in the readiness report §1. Gate table with exit codes:
[`docs/status/CUTOVER-READINESS.md`](../status/CUTOVER-READINESS.md) §1.

Summary: **31 gates exit 0.** Two exit non-zero — `check:phase8` (1, the
performance gate, §7) and `npx eslint src scripts` (1, 23 pre-existing errors,
none in code this round touched, and not a CI gate since CI lints with Biome).

**CI IS GREEN**, watched to conclusion on five consecutive commits: `0b6e366`,
the merge commit `66a5a91`, `075e2bf`, `abc8e66` and `15c6b99`. Every gate
passes, including all four wired in this round. One step is skipped by design
(`Assistant link integrity`, awaiting the API key) and its companion reporting
step runs; the only other skip is the failure-conditional artefact upload. One step is skipped by design —
`Assistant link integrity`, awaiting `ANTHROPIC_API_KEY` — and its companion step
runs and prints a warning naming the gate and its owner.

Two wrong turns on the way there, both recorded in §5.
