# Relay v22-B — Round 14 session B, launcher accessibility, Person schema, ThemeToggle

**Branch** `feat/round14-surfaces` · **Worktree** `../yallo-talent-website-B` · **Port** 3214 · **NEXT_DIST_DIR** `.next-b-r14`
**HEAD** `fc1530747259f2b78b366bfdce60400387928060`

---

## Shipped

- **Launcher keyboard distance, §2.3.** Measured 77–84 Tab stops from a fresh homepage load (round 13's advisory, now ruled a real barrier). `AssistantLauncherMount` moved in `src/app/layout.tsx` from last child of `<body>` (after `Footer` and `StickyBriefCTA`) to right after `NavBar`, before `<main>`. Fixed-position, so DOM order carries no screen position — re-measured at **10 Tab stops** on the homepage at 1280/light, zero visual change (screenshots below). `check-assistant-a11y.mjs`'s stale "appended last in the DOM" comment corrected to match.
- **Launcher/StickyBriefCTA collision, §2.4.** First time both were visible in one viewport (A's mount seam hadn't landed in round 13). Measured a real overlap at 360px: launcher's mobile circle `{x:12, y:662, w:54, h:54}` against StickyBriefCTA's panel `{x:12, y:672, w:336, h:116}` — a 54×44px overlap in the bottom-left corner, not the "opposite corners, never seen together" state round 13 assumed. Per §2.4's failure branch, the launcher yields: `AssistantLauncher.tsx` now watches for StickyBriefCTA's own DOM presence (`[aria-label="Contact CTA"]`) via `MutationObserver` + `matchMedia("(max-width: 640px)")`, using the same `useSyncExternalStore`-over-external-signal pattern `ThemeToggle.tsx` already uses, and renders nothing when both conditions hold. **`StickyBriefCTA.tsx` itself was not touched** — no edit, no export added, nothing. Re-verified after the fix: 0 launcher instances in the DOM at 360px while the CTA is showing, both present with no overlap at 1280px, both themes.
- **`Person` schema, §2.5.** `leadershipPersonJsonLd()` added to `src/lib/jsonld.ts`, mapping `teamIndex` (`src/data/team/index.ts`) to one `Person` node per leader. Four fields only — `name`, `jobTitle`, `url`, `worksFor` — rendered on `/leadership` for all five real named leaders. `url` resolves to a real fragment (`id={member.slug}` added to each leadership card, verified resolvable) rather than an invented address. No `email`, `telephone`, `knowsAbout`, specialism, biography, seniority claim or years-of-experience figure — `teamIndex` carries none of those fields, so there was nothing to hand-type even under time pressure.
- **`ThemeToggle`, §2.5(b).** Measured first rather than assumed: `ThemeToggle.tsx` was fully built and fixed in round 10 (comment on the file said so explicitly) but nothing in `src/` imported it, so it rendered on no page across rounds 10–13. The blocking assumption in the stale `NavBar.tsx` comment — "hidden until light theme is production-ready" — was itself wrong; `src/config/theme.ts` has called light and dark peers, each independently AA, since round 10. Mounted in `NavBar.tsx`: header actions row above 1024px, mobile drawer below it (own "Theme" row, next to Jobs and Start a brief). **Not both at once** — added to the header first, then measured the hamburger at `x:362.6` (2.5px off the 360px canvas) because the sub-480px bar is tuned to a documented zero-slack pixel budget. Retracted that placement (see Retractions) and scoped the header instance to desktop-only via a `1024px` media query matching where `.primary` already collapses to the hamburger, relocating the mobile instance into the drawer where the width fight doesn't exist. Verified both instances toggle the same shared `data-theme` state correctly (light→dark→light) via a headless click test.

## Not reached

- **`check-a11y.mjs`'s route-derivation fix (§2.2).** Not in this session's brief (the `/goal` dispatch scoped items 1–3 to B; §2.2 names no session but the script itself is A's per §3.1 item 3 — B consumes it read-only). Ran it as-is against `/` and `/leadership` post-change: clean, exit 0, only the pre-existing `__ghost` exemption. No new failures from this round's changes.
- **Transcript persistence and its retention purge (§2.1).** A's territory (`src/lib/db/**`), carried from round 13, still open.
- **A design pass on the drawer's new "Theme" row.** Shipped as a minimal label + existing `ThemeToggle` button, matching the drawer's existing row rhythm (border-bottom, `--fs-body-sm`). Not run through a full Impeccable review as its own surface — flagged as an open item below rather than assumed acceptable.

## Decisions taken under delegated authority

1. **`url` on the Person schema points to `${SITE.url}/leadership#{slug}`, not a LinkedIn URL.** The authorised field list is `name`, `jobTitle`, `url`, `worksFor` only — no `sameAs` — so LinkedIn couldn't carry the identity-corroboration role it plays on `organisationJsonLd()`. Added `id={member.slug}` to each leadership card so the fragment is a real, resolvable address rather than an invented one. **Reversible, additive-only** — one attribute per card, no markup restructuring.
2. **The launcher watches StickyBriefCTA's DOM presence rather than re-deriving its `SHOW_AFTER_PX`/`HIDE_NEAR_END` thresholds.** A copied threshold is the exact defect class this codebase keeps finding (a second source of truth that drifts from the first), and it also let `StickyBriefCTA.tsx` go completely untouched, honouring "never move, resize or restyle" by the widest possible margin. **Reversible** — confined to `AssistantLauncher.tsx`; removing the `useSyncExternalStore` call restores the round 13 behaviour exactly.
3. **`ThemeToggle` mounted twice (header + drawer) rather than once with a responsive relocation of the same DOM node.** Two independent subscribers to the same external store (the `data-theme` attribute) rather than one node moved by CSS, because the header and drawer are genuinely different DOM subtrees (drawer is a separate `motion.div`, not a media-query variant of the header). **Reversible** — deleting either mount point removes exactly one control with no effect on the other.
4. **Retracted the header-only placement mid-session after measuring the overflow** — see Retractions. Not treated as done until re-measured at 0px overflow.

## What I did not do, and why

- **Did not edit `StickyBriefCTA.tsx`, `.claude/launch.json`, `scripts/check-a11y.mjs`, or any file outside `src/app/layout.tsx`, `src/components/**`, `src/lib/seo.ts`/`jsonld.ts`, `src/data/leadership*`, and my own `check-assistant-*.mjs`.** All A's territory or explicitly protected. Ran `pnpm install` (no lockfile/`package.json` diff resulted — confirmed via `git status` before and after) because this worktree's `node_modules` was empty; did not touch the lockfile by hand.
- **Did not add a `session-b-r14` entry to `.claude/launch.json`.** A's file this round (§3 territory table). Ran the dev and production servers directly with `NEXT_DIST_DIR=.next-b-r14` and `-p 3214` instead of through a launch-config name. Logged below as a need.
- **Did not build a candidate-facing theme control or extend `ThemeToggle`'s own behaviour.** Out of scope — the task was mounting an already-correct component, not redesigning it.
- **Did not run Lighthouse or `check:phase8`.** Forbidden this round (§4) — two sessions building on one machine invalidate the number.

## Retractions

1. **The first `ThemeToggle` header mount overflowed the page at 360px.** Added it to `.actions` alongside the existing CTA and hamburger, then measured (not assumed) the result: hamburger bounding box at `x:362.6, width:36` — 2.5px past a 360px canvas, because `NavBar.module.css`'s own comments document that bar as tuned to a zero-slack pixel budget with no spare width left. **Retracted that placement entirely.** Fixed by hiding the header instance below 1024px (matching where `.primary` already collapses) and adding a second instance to the mobile drawer, where the same measurement afterward showed `x:320.6, width:36` — comfortably inside 360px, zero horizontal overflow (`document.documentElement.scrollWidth - clientWidth === 0`) at both 360 and 1280, both themes.
2. **Nothing else measured wrong this session** — the tab-stop re-measurement, the collision measurement, and the Person schema field check all matched on first measurement, so there is nothing else to retract here. Recorded as empty-but-checked rather than omitted, per the standing instruction that this section is required even when there is little in it.

## Anything needed from A's territory

1. **`.claude/launch.json`** — needs an additive `session-b-r14` entry (`NEXT_DIST_DIR=.next-b-r14 pnpm dev -p 3214`), matching the `session-a-r13`/`session-b-r13` pattern already there. Not added by this session (A's file); this session ran the equivalent command directly instead.
2. **Transcript persistence (§2.1), carried from round 13** — still blocks the pilot flag turning on. Nothing in this round's B-territory work depends on it, but it's still the standing blocker.

## Open items, each with its exact unblocking question

1. **Specialism and contact route per named leader (§2.5, carried).** The Person schema and the `/leadership` cards both have real slots now (`teamIndex`, the schema's own field list) but no content to put in most of them — 4 of 5 leaders have no `bio`, none has a `linkedin`-equivalent field the schema is authorised to use. *Unblocking question: for each of the five, is there a one-sentence specialism/`knowsAbout` string and a contact route (e.g. their own LinkedIn as a future `sameAs`, or a shared inbox) you want published? Absent is the correct default until you say otherwise — this is not a request to fill a gap, it's confirming the gap is intentional.*
2. **The drawer's new "Theme" row hasn't had a design pass of its own.** It reuses the drawer's existing row pattern (border-bottom, body-sm label) rather than being designed as a first-class row. *Unblocking question: is the plain label-plus-toggle treatment fine as shipped, or does this warrant a uiux-emil pass before it's considered finished rather than functional?*

## Risks

- **The launcher's collision-avoidance depends on StickyBriefCTA's `aria-label="Contact CTA"` staying exactly that string.** If a future round changes that label (even for a good reason, e.g. localisation), the `MutationObserver`'s selector silently stops matching and the two surfaces can overlap again with no gate catching it — `check-assistant-a11y.mjs` only tests the launcher's own route (`/`) and doesn't scroll far enough to trigger StickyBriefCTA. No gate exists for this collision at all; this relay's own measurement is the only evidence it doesn't happen, not a standing check.
- **`ThemeToggle` mounted twice increases the size of the "did both instances actually receive the click" surface**, though the shared `useSyncExternalStore` subscription means there is exactly one source of truth (`data-theme`) and no route for the two to disagree — verified by clicking each instance independently and reading the attribute back, not assumed from the pattern alone.

## Notes for A

- **`layout.tsx` stayed exclusively mine this round per §3.1 item 1** — no seam, no coordination needed. The only change there is the `AssistantLauncherMount` reorder; `StickyBriefCTA`, `Footer`, `NavBar` and the `<main>` block are otherwise untouched.
- **`check-a11y.mjs` ran clean, read-only, against `/` and `/leadership`** post-merge-worthy — exit 0, one pre-existing exemption, nothing new. No changes needed on your side for this round's B work to be compatible with it.
- **`pnpm install` regenerated this worktree's `node_modules`** (it was empty) with no lockfile diff — confirmed via `git status` immediately before and after. Nothing for you to reconcile at merge.
- **`.claude/launch.json` needs `session-b-r14`** whenever convenient — see "Anything needed from A's territory" above.

## Gates run, and the real exit code of every one

| Gate | Exit | Notes |
|---|---|---|
| `tsc --noEmit` | 0 | |
| `npx biome check` (9 touched files) | 0 | One import-order fix applied by hand (not `--write`, to avoid the repo-wide rewrite this worktree's memory warns about); clean after. |
| `pnpm build` (`NEXT_DIST_DIR=.next-b-r14`) | 0 | `tsconfig.json`'s dist-dir rewrite reverted with `git checkout --` after every build/dev run, confirmed via `git status` before staging each commit. |
| `check:assistant-a11y` (`NEXT_PUBLIC_ASSISTANT_ENABLED=true`, against `pnpm start` on 3214) | 0 | 4 advisory (the pre-existing hand-resolved `color-contrast` abstention at 8.74:1), 0 blocking. Re-measured tab-stop count via the same gate's own logic: 10 stops (down from 77–84), well under its 60-stop advisory threshold — no advisory fired. |
| `node scripts/check-terminology.mjs` | 0 | No banned terminology in 263 files. |
| `node scripts/check-a11y.mjs --routes /,/leadership` (A's gate, read-only) | 0 | axe clean across 2 routes × 2 themes × 2 widths; 1 pre-existing exemption applied (`__ghost`), 284 abstained contrast nodes composed by hand (unrelated to this round's changes). |

All against a production build (`NEXT_DIST_DIR=.next-b-r14 pnpm build && pnpm start -p 3214`) except the two `tsc`/`biome` static checks. No gate reported here was not actually run and its exit code observed.

---

## Addendum — live demo, same session, HEAD now `d4cdee62cd926cef0c29e561dcfdad566fc701ac`

Sumeet ran the flag-on build live (temporary, env-var only, never the committed default) to demo to his team, and worked the assistant end to end rather than reading a relay. Six more commits came out of that, each already gated and screenshotted before landing:

1. **`53c311f`** — chat errors existed in state but only ever reached a screen-reader-only live region; a sighted user saw nothing happen on failure. Now a real bubble in the message log.
2. **`18a03e6`** — the launcher disclosure was three lines and, worse, false: it claimed 12-month retention while no transcript store exists (still true — see Anything needed from A's territory). Shrunk to one line, reworded to what's actually true today. Self-caught a real regression fixing this: the first single-line attempt pushed the close button behind the panel's own `overflow: hidden` at 360px, present in the DOM but invisible on screen.
3. **`ea31b7a`** — replies are ordinary markdown from the model (bold, lists, paragraphs); the panel rendered all of it inside one `<p>`, so newlines collapsed and `**bold**` showed literally. Small hand-written renderer, not a dependency (`package.json` is A's this round).
4. **`cb0761c`** — two bugs Sumeet found live: citations rendered as bare `/eor`-style text once markdown landed, never as links; and the brief confirmation card printed the model's raw tool-call codes (`me`, not "Middle East"). Both fixed — see "Open for Chat" below for the part of this that wasn't.
5. **`4f99d18`** — swapped which corner the launcher and `StickyBriefCTA` anchor to, direct instruction. Placement only; `StickyBriefCTA`'s own behaviour untouched.
6. **`d4cdee6`** — full-height slider on desktop (≥641px), the existing small popup unchanged below that; no backdrop, page stays interactive. Self-caught a second regression: the first version buried the site's own sticky header, fixed by anchoring to `var(--header-h)` rather than `top: 0`.

### Open for Chat — flagged by Sumeet himself, not decided here

**Region: coarse label shipped; per-country capture is not, on his instruction.** He asked for the exact place typed ("UAE") to survive alongside the coarse region bucket, then added: *"If this is broader than the chatbot or if it collides with Start a Brief CTA's data model, then don't make this change. Write it in your final relay and let chat decide on that."* It does collide: `region` is `briefFormSchema`'s enum, shared field-for-field with the Anthropic tool schema in `src/lib/assistant/client.ts` and validated again by A's `/api/brief` — the same four codes (`uk`/`me`/`india`/`multi`) `/brief`'s own `<select>` uses. Adding a country-level value means either widening that enum (a contract change reaching A's route and the canonical form, not just the assistant) or adding a second, separate free-text field alongside it (new field on the brief payload, which A's validation would need to accept too). Genuinely a scope call for a future round, not a fix a live session should make unilaterally. Nothing built for this; the coarse `regionLabel()` fix in `cb0761c` is what shipped.

### Still open, unchanged from the base relay

**`DATABASE_URL` and the three `RESEND_*` vars are unset in *both* worktrees**, not just this one — checked directly, not assumed. Different from the Anthropic key: there was nothing anywhere on this machine to copy across this time. `/api/brief` fails closed exactly as designed ("Could not record your brief"), which is correct behaviour, not a bug — round 13's own capture-first architecture refuses to report success without a persisted row. Sumeet needs to paste real values into `.env.local` himself (never through this session) before the backend half of the brief flow — persist, then email both recipients — can be tested for real. Flagged to him directly: once wired, every test submission sends a real email and writes a permanent record, fake data included.

### Gates run this addendum, and the real exit code of every one

| Gate | Exit | Notes |
|---|---|---|
| `tsc --noEmit` | 0 | Run after every commit in this addendum, not once at the end. |
| `npx biome check` (each touched file, never repo-wide `--write`) | 0 | One import-order fix applied by hand early in the base round; nothing further needed here. |
| `pnpm build` (`NEXT_DIST_DIR=.next-b-r14`) | 0 | Six times, once per commit. `tsconfig.json` reverted after every run. |
| `check:assistant-a11y` (against `pnpm start` on 3214) | 0 | Run after every commit; same 4 pre-existing advisory items each time, 0 blocking throughout. |
| `check:terms` | 0 | 265 files. |
| `check:a11y --routes /,/leadership,/brief` (A's gate, read-only) | 0 | Added `/brief` this pass specifically to confirm the `briefLabels.ts` extraction didn't regress the canonical form. Clean, 1 pre-existing exemption. |

No gate reported here was not actually run against a live server with its real exit code observed. Screenshots for every fix (light/dark, 360/1280 where relevant, plus real model output for the markdown/link/region fixes) were taken and reviewed before each commit, then deleted from the scratchpad — none are committed to the repo.

---

## Addendum 2 — closing this session. HEAD now `5352604a77d2f3ff295a915b4394668e7606fbd6`

Sumeet provisioned real credentials mid-session and asked for the full backend loop tested for real, then separately asked for a domain-policy correction, then closed out an open cost question. Six more commits.

### Shipped

- **`93f3601`** — the desktop drawer's entrance felt "too aggressive" (Sumeet's words, on seeing it live). Root cause: the panel's full height exists from frame one (only `x`/`opacity` animate), so a 24px nudge over 0.2s read as materialising rather than sliding in. Changed to `x: "100%"` (self-relative — one value that slides the desktop drawer in from fully off-canvas and the mobile popup in proportionately, no breakpoint branch) over 0.28s. Measured across sampled frames, not assumed smooth: panel starts ~925px off its resting position, travels through multiple frames, settles correctly. A two-stage half-height-then-full-height alternative was discussed and explicitly declined by Sumeet in favour of this — logged as a considered-and-set-aside option, not a retraction, since nothing of it was built.
- **Real credentials wired and tested end to end.** `DATABASE_URL` and `RESEND_API_KEY` landed in this worktree's `.env.local` (after a detour below), migration applied (`0001_submissions.sql`, via `pnpm db:migrate`), production build restarted. Ran a real brief through the assistant with data clearly marked `TEST VERIFICATION — please ignore`: confirmed via direct query against the live `submissions` table (`endpoint: "brief"`, `source: "assistant"`) and `delivery_status.email.delivered: true`. Both halves of round 13's capture-first architecture verified working, not assumed from a 200 response.
- **`458d041`** — `RESEND_TO` only ever sent to one address (`to: [RESEND_TO]`, a single-element array) despite round 13's own spec saying both `brief@yallo.co` and `hello@yallo.co` should receive it. Sumeet named this explicitly: "only these two are allowed." Fixed in both `api/brief` and `api/cv` (same bug, same fix, same class) — comma-separated default, split into a real array, future env overrides can name more without a second code change. Same commit drops the `talent.yallo.co` sender default on both routes.
- **`611db7e` / `5352604`** — Sumeet: "we are not setting up a talent subdomain" — yallo.co itself is Yallo Talent's domain, talent.yallo.co is a pre-cutover placeholder only, permanently. Swept every reference: `src/data/legal/{privacy,terms,cookies}.ts` (six places — the actual, currently-wrong copy, not stale docs, since `SITE.url` already resolves to yallo.co), the three legal pages' own `<meta description>` tags (a second, independently-drifted copy of the same line), `corpus.ts`'s own hand-typed summaries (same drift, different surface), the Yallo Group footer link, and three project docs (`README.md`, `AGENTS.md`, the architect subagent's role line). `AGENTS.md` gets a short paragraph recording the ratification itself, not just the domain swap, so a future session reads why. Left `scripts/check-crawler-access.mjs`, `robots.ts`, and `AssistantLauncherMount.tsx`'s comment untouched — all three correctly describe talent.yallo.co as what it actually still is today, a noindexed pre-cutover placeholder, which is the exact distinction this whole change protects.
- **Caught in passing, same commit**: `privacy/page.tsx`'s assistant section claimed conversations are "recorded and kept for 12 months, then deleted" — the identical false retention claim already found and fixed in the panel's own disclosure line earlier this session (`18a03e6`). Corrected to match.
- **Model cost question, resolved without a code change.** Sumeet asked to default to "Sonnet 4.6" for cost; verified against Anthropic's own docs rather than memory (org policy) before answering. Sonnet 4.6 is real but priced at $3/$15 per MTok — *more* than Sonnet 5's current introductory rate ($2/$10 through 31 Aug 2026) and exactly equal to Sonnet 5's post-introductory price. It would not have solved the stated problem. Haiku 4.5 genuinely would (roughly half the cost, including cached-read rate), but switching reopens `client.ts`'s own ratified "One model, Sonnet 5. No router, no cheap-tier triage" line, for a real reason (refusal-boundary risk on a page whose failure mode is a fabricated claim about a person or a leaked rate). Recommended testing Haiku against the refusal suite before defaulting to it rather than switching blind. **Sumeet's decision: keep Sonnet 5.** `client.ts` was never edited — verified unchanged (`const MODEL = "claude-sonnet-5"`) rather than assumed.

### Retractions

1. **A real credential exposure, self-inflicted, disclosed immediately.** Diagnosing why `DATABASE_URL` looked empty across both worktrees, a `sed` command I ran to redact `.env.local` for display failed to actually redact anything — it wrapped the full raw `ANTHROPIC_API_KEY` value in decoration characters while leaving the value itself intact, printing the complete key into this session's own tool output. Flagged to Sumeet in the same turn, before doing anything else. He rotated the key. No further command in this session ever echoes a `.env.local` value again — every subsequent check went through length-only `awk`, or values read directly inside a Node subprocess and passed straight into a child's `env` without ever passing through a shell string or a `console.log`.
2. **The `DATABASE_URL`/credential mystery had a mundane cause, found by measurement, not assumed.** Two rounds of "I set it, it's not there" traced to: first, Sumeet editing the file in Cursor without saving (`•` unsaved-changes dot on the tab, confirmed by the file's own unchanged mtime); second, once saved, a Postgres connection string's unescaped `&` breaking a plain shell `source .env.local`. Neither was this session inventing excuses — both were checked (mtimes, a controlled parse error) before being reported as the cause.

### What I did not do, and why

- **Did not build region-per-country capture.** Sumeet explicitly deferred this to Chat's adjudication in Addendum 1; nothing changed here.
- **Did not touch `package.json`'s `"name": "talent.yallo.co"` field.** `package.json` is A's territory this round. Logged as a spawned task instead of edited directly (task id `task_a0346f96`).
- **Did not touch the `assets/client-logos/` changes found sitting in the working tree** (`Absolute Labs.png` deleted, `informatica.png`/`Sephora.png` modified, `BlueYonder.jpeg` added, still uncommitted at session close). Mtimes and a freshly-touched `.DS_Store` in the same folder point to Sumeet editing directly in Finder/Cursor, not this session or a conflicting process — flagged to him, not committed, not discarded. **Still open — see below.**
- **Did not run Lighthouse or `check:phase8`.** Forbidden this round throughout, unchanged from the base relay.

### Open items, each with its exact unblocking question

1. **The `assets/client-logos/` working-tree changes are still uncommitted.** *Is this your in-progress asset work (add Blue Yonder, replace/rename Informatica, remove Absolute Labs), and if so do you want it committed as its own commit, or left for you to commit yourself?* Not touched either way pending an answer.
2. **Region-per-country capture** (Addendum 1) — Chat's call, per Sumeet's own instruction. Unchanged, still open.
3. **`RESEND_FROM`/`RESEND_TO` are not explicitly set in `.env.local`** — the code's new defaults (`brief@yallo.co` sender, both aliases as recipients) are doing the work. Fine as shipped, but *if Sumeet wants different values in production, they need setting explicitly* — the defaults are a sensible fallback, not a production decision recorded anywhere else.
4. **Carried from the base relay**: transcript persistence (still unbuilt, still the pilot-gate blocker), `.claude/launch.json`'s `session-b-r14` entry (still A's to add).

### Risks

- **The Anthropic key that was briefly exposed has been rotated**, per Sumeet's confirmation, but the old value did pass through this session's tool output before rotation. Recorded here for the same reason round 13 recorded its own version of this: so it's traceable, not because there's an outstanding action.
- **Every credential in `.env.local` in this worktree is now real** — a live Neon database, a live Resend account capable of sending real mail to `brief@yallo.co` and `hello@yallo.co`. The demo server on port 3214 is still running with the assistant flag on for Sumeet's team demo. Anyone testing the brief flow from here on writes a real, permanent row and sends a real email — worth remembering before further ad hoc testing, by Sumeet or by a future session.

### Gates run this addendum, and the real exit code of every one

| Gate | Exit | Notes |
|---|---|---|
| `tsc --noEmit` | 0 | Run after every commit. |
| `npx biome check` (each touched file) | 0 | One formatting fix applied by hand (a shortened string that now fit on one line). |
| `pnpm build` (`NEXT_DIST_DIR=.next-b-r14`) | 0 | Once per commit; `tsconfig.json` reverted every time. |
| `pnpm db:migrate` | 0 | Against the real Neon database, via a Node subprocess with `DATABASE_URL` injected directly into `env` — never through a shell string. `0001_submissions.sql` applied. |
| Real end-to-end brief submission | — | Not a gate script; verified directly by querying `submissions` for the test row and reading `delivery_status`. `delivered: true`. |
| `check:assistant-a11y` (against `pnpm start` on 3214) | 0 | Same 4 pre-existing advisory items, 0 blocking. |
| `check:terms` | 0 | 265 files. |
| `check:a11y --routes /,/leadership,/brief,/privacy,/terms,/cookies` (A's gate, read-only) | 0 | Added `/privacy`, `/terms`, `/cookies` this pass specifically to confirm the domain-reference edits didn't regress those three pages. Clean, 1 pre-existing exemption, across 6 routes × 2 themes × 2 widths. |

No gate reported here was not actually run and its exit code observed. This closes the session at `5352604`.
